# NotchFlow 第四阶段技术开发文档｜设置、权限与恢复

> 文档版本：v1.0
> 对应 PRD：阶段 3｜设置、权限与恢复
> 当前分支：`phase/04-settings-permissions-recovery-complete`
> 当前状态：第四阶段 4.1—4.4 全部完成并通过人工出口

---

## 1. 阶段目标

本阶段把前三阶段已验证的刘海交互和四个核心模块，补齐为可长期使用、可配置、可恢复的本地 macOS 应用。

本阶段交付：

- 设置窗口和菜单栏快捷入口。
- 四个核心功能的独立开关与渐进权限状态。
- 多显示器、全屏和合盖后的正确迁移策略。
- 设置、计时器和文件架的持久化及异常恢复。

本阶段不做：

- DMG、签名、公证、Mac App Store 上架。
- 云同步、账号、埋点、服务器或 AI 能力。
- Spotify 正式适配。
- 依赖非公开接口替换系统 HUD。

## 2. 开发段划分

### 4.1 设置窗口与菜单栏

- 创建菜单栏图标和快捷菜单。
- 支持打开设置、展开/收起刘海、暂停 1 小时、暂停至明天、立即恢复和退出。
- 通用设置支持开机启动和菜单栏图标显示。
- 暂停期间隐藏刘海并停止音乐、系统状态轮询；计时器继续按目标结束时间运行。
- 设置和暂停截止时间写入 `UserDefaults`，重启后恢复。

人工出口：菜单栏入口、设置窗口、展开/收起、暂停/恢复均能真实操作。

验证结果：通过。菜单栏与设置窗口由产品经理确认；菜单栏图标开关、两种暂停、立即恢复、暂停跨重启恢复和开机启动由 AI 在真实 Mac 上完成操作。详见 `第四阶段开发段4.1验证报告-NotchFlow-设置与菜单栏-v1.0.md`。

### 4.2 功能开关与渐进权限

- 音乐、文件架、系统状态、计时器可独立开关。
- 关闭模块后停止对应轮询或交互入口。
- 设置页展示自动化、通知、文件访问和增强 HUD 的真实状态。
- 权限只在用户使用对应功能时申请；拒绝不影响其他模块。

人工出口：逐一关闭和开启功能，确认界面、轮询和权限行为同步变化。

验证结果：通过。产品经理已确认设置窗口及“通用 / 功能 / 权限”三个分页显示正常；AI 在真实 Mac 上验证四个功能入口能够独立消失和恢复，并确认设置已恢复为验收前状态。详见 `第四阶段开发段4.2验证报告-NotchFlow-功能开关与权限中心-v1.0.md`。

### 4.3 显示器、全屏与合盖

- 支持内置屏、主显示器和指定显示器策略。
- 屏幕插拔、分辨率变化和合盖后重新计算位置。
- 实现“始终显示、仅重要提醒、完全隐藏”全屏策略。
- 无刘海屏幕保持顶部悬浮胶囊降级。

人工出口：普通桌面、全屏和可用的多显示器场景均不残留错位面板。

开发结果：已完成。目标屏幕支持“内置屏幕 / 主显示器 / 指定显示器”，显示器使用稳定 UUID 保存；目标消失和合盖场景按当前主显示器回退。设置页已加入三档全屏策略，以及无刘海屏幕的顶部间距和宽度微调。应用监听屏幕参数、Space 与前台应用变化，并重新判断位置和可见性。

自动验证：57 项 XCTest 全部通过；当前 Mac 的内置屏解析、默认普通桌面可见窗口和物理刘海 209×39 面板均通过本机运行检查。外接屏拔插与合盖因当前没有对应硬件场景，继续保留人工补验。详见 `第四阶段开发段4.3验证报告-NotchFlow-显示器全屏与合盖-v1.0.md`。

人工结果：通过。产品经理已确认真全屏时浮层隐藏、退出真全屏后收起胶囊自动恢复，且没有残留错位面板。外接屏与合盖继续标记为有设备时补验。

### 4.4 异常恢复与阶段出口

- 校验并修复失效设置值。
- 权限撤回、播放器退出、文件索引异常、目标屏幕消失均安全降级。
- 验证应用重启后设置、暂停状态、计时器和文件架恢复。
- 完成本阶段自动化测试与真实 Mac 回归。

人工出口：强制退出并重开 App 后，用户数据和可见状态符合预期。

开发结果：已完成。设置读取后会将类型错误、非法枚举、空显示器标识和越界数值修正为安全值并写回；计时器 JSON 损坏或字段组合不合法时会清除坏状态，合法的运行中状态继续按目标结束时间恢复；文件架索引损坏时会扫描 NotchFlow 自有暂存目录并原子重建索引；Apple Music 权限撤回、播放器退出和目标屏幕消失均只影响对应能力并安全降级。

自动与运行验证：64 项 XCTest 全部通过，覆盖设置、暂停、计时器与文件架跨控制器恢复，以及坏数据修复和权限撤回降级。真实 4.4 App 已完成一次 `kill -9` 强制结束与重新启动，新进程正常建立，物理刘海浮层恢复为 `209 × 39`、坐标 `X=651 / Y=0`。详见 `第四阶段开发段4.4验证报告-NotchFlow-异常恢复与阶段出口-v1.0.md`。

人工结果：通过。AI 重新建立 5 分钟计时后强制结束并重启 App，恢复界面显示 `04:28`，证明倒计时按原目标时间连续恢复而非从 `05:00` 重新开始；产品经理已确认刘海上的绿色计时图标与倒计时可见。

## 3. 技术方案

- 原生 Swift、SwiftUI、AppKit，不增加第三方依赖。
- 普通设置使用带默认值和取值校验的 typed `UserDefaults`。
- 开机启动使用公开的 `SMAppService.mainApp`。
- 菜单栏使用 `NSStatusItem`，设置页继续使用普通 `NSWindow`。
- 暂停状态由应用级控制器统一调度，不在各视图散落布尔值。
- 显示器匹配以 `NSScreen` 当前能力和稳定标识为准，目标消失时回退主显示器。

## 4. 数据与状态

第四阶段新增的设置均必须满足：

- 有明确默认值。
- 读取旧值时校验范围；无效值回退默认值。
- 写入后立即影响运行态。
- 不保存密码、账号、文件内容或完整隐私路径。

暂停状态：

- `active`：刘海和已启用模块正常工作。
- `paused(until)`：刘海隐藏，非必要轮询停止，菜单栏保留恢复入口。
- 截止时间到达：自动回到 `active`。
- 读取到已过期截止时间：清除旧值并正常启动。

## 5. 测试与验收原则

本项目不调用模型，本阶段采用：

1. XCTest：设置默认值、持久化、过期恢复、模块开关、屏幕策略和异常降级。
2. 真实 Mac 验收：菜单栏、设置窗口、系统设置跳转、全屏、屏幕变化和重启恢复。

每个开发段完成后单独给出一次人工验证步骤；人工确认前不进入下一开发段。

## 6. 风险与降级

- 用户隐藏菜单栏图标后，仍可通过刘海右键菜单打开设置。
- 用户暂停 NotchFlow 时保持菜单栏图标，避免失去恢复入口。
- 开机启动注册失败时展示真实错误，不冒充已开启。
- 无法稳定获取的亮度状态继续标记不可用，不显示猜测数值。
- 缺少外接显示器时，多屏与合盖只标记待真实设备补验，不用模拟结果冒充。

## 7. 阶段出口

- PRD 阶段 3 的设置、权限、恢复和显示器要求均有实现或明确降级。
- 自动化测试全部通过。
- 每个开发段的人工验收均由产品经理确认。
- 创建 `phase/04-settings-permissions-recovery-complete` 完成分支后，才进入验收与发布准备阶段。

阶段结论：上述出口均已满足，第四阶段完成。外接屏拔插与合盖因当前无对应设备，继续作为发布前硬件补验项，不影响本机第四阶段出口。
