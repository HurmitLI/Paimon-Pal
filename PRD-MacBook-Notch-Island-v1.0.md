# Paimon Pal（简称 PP）产品需求文档

> 文档类型：MVP PRD  
> 文档版本：v1.1
> 状态：已确认 Mac 不遮挡优先，S4/S6 交互规格完成修订
> 更新日期：2026-08-26  
> 适用平台：macOS 14.0 及以上  
> 产品形态：原生 macOS 菜单栏应用 + 刘海紧凑交互 + 普通功能窗口

---

## 1. 产品概述

### 1.1 一句话定义

Paimon Pal 是一款以派蒙本地陪伴为核心，并利用 MacBook 屏幕刘海区域提供音乐控制、文件暂存与 AirDrop、系统状态提醒和计时器能力的本地原生工具。

### 1.2 产品定位

Paimon Pal 不是把一个完整控制中心塞进屏幕顶部，而是让派蒙陪伴与需要随时看一眼、随手操作一下的高频信息共同出现在刘海附近。

产品采用“苹果灵动岛交互语义 + MacBook 实际刘海尺寸自适应”的设计：

- 信息收起时贴合物理刘海，不额外占用工作区。
- 临时状态在刘海左右两侧短暂出现。
- 悬停时提供轻量预览。
- 点击后只在刘海两翼显示紧凑快捷控制，不向下展开大浮层。
- 文件架、设置等复杂内容使用可移动、不置顶的普通窗口。
- 外接无刘海显示器时，自动显示为顶部悬浮胶囊。

### 1.3 核心价值

1. 让原本不可用的刘海区域成为高频快捷入口。
2. 减少在音乐应用、Finder、控制中心和计时器之间来回切换。
3. 保持低打扰：需要时出现，不需要时与物理刘海融为一体。
4. 本地优先：不上传文件，不采集使用数据，不依赖账号体系。

---

## 2. 已确认产品决策

| 决策项 | 已确认方案 |
|---|---|
| 产品形态 | 原生 macOS 应用，不采用网页套壳 |
| 技术框架 | Swift + SwiftUI + AppKit |
| 窗口实现 | 无边框透明 `NSPanel`，固定在屏幕顶部中央 |
| 尺寸策略 | 交互逻辑参考苹果灵动岛，收起尺寸匹配 MacBook 实际刘海 |
| 首版核心功能 | 音乐控制、文件暂存/AirDrop、系统状态、计时器 |
| 数据策略 | 本地保存，不做云同步和行为追踪 |
| 外接屏策略 | 无刘海显示器显示顶部悬浮胶囊 |
| 首版分发 | 先完成本地构建与签名 DMG；Mac App Store 适配不作为 MVP 阻塞项 |
| 开源参考边界 | 可参考 MIT 项目；不直接复制 GPL/AGPL 项目源码进入闭源产品 |

### 2.1 命名与暂定项

- 产品正式名称为 **Paimon Pal**，简称 **PP**；`NotchFlow` 仅作为兼容旧版本的内部工程与数据目录名称保留。
- 首版优先适配 Apple Music 与 Spotify；其他播放器根据技术验证结果逐步扩展。
- 是否上架 Mac App Store，在音乐信息获取和系统 HUD 权限验证后决定。

---

## 3. 用户与使用场景

### 3.1 目标用户

- 使用带刘海 MacBook 的办公、学习和创作用户。
- 经常听音乐，需要快速暂停、切歌或查看当前曲目的用户。
- 经常在应用之间临时搬运文件或使用 AirDrop 的用户。
- 希望快速查看充电、电量、音量、亮度状态的用户。
- 需要轻量倒计时，但不希望频繁打开专门计时器应用的用户。

### 3.2 核心场景

#### 场景 A：音乐控制

用户正在写文档，鼠标移到刘海区域即可看到当前歌曲；点击后展开播放器，完成暂停、切歌和拖动进度，无需切换到音乐应用。

#### 场景 B：临时文件暂存

用户从 Finder 或聊天应用拖动一个文件到刘海，刘海自动变成接收区。文件被复制到本地暂存区，稍后可从刘海拖出或通过 AirDrop 发送。

#### 场景 C：系统状态反馈

用户插入电源或调整音量时，刘海短暂显示状态和数值，随后自动收起，不遮挡当前内容。

#### 场景 D：计时器

用户从展开面板快速启动一个计时器。收起后，刘海右侧持续显示剩余时间；结束时通过动画和系统提示音提醒。

#### 场景 E：外接显示器

用户将 MacBook 连接到无刘海显示器，功能自动切换为顶部居中的黑色悬浮胶囊，交互方式保持一致。

---

## 4. 产品目标与非目标

### 4.1 MVP 目标

1. 在带刘海 MacBook 上稳定、准确地贴合物理刘海。
2. 四个核心功能形成完整闭环，而不是只完成视觉演示。
3. 不使用功能时保持安静，不影响菜单栏、全屏和多显示器工作流。
4. 核心功能在不登录账号、不联网的情况下可用。
5. 提供清楚、渐进式的权限说明，拒绝权限后仍可使用不相关功能。

### 4.2 MVP 非目标

以下能力不进入首版开发范围：

- 天气。
- 日历和待办。
- 剪贴板历史。
- AI 命令栏。
- 通知中心消息镜像。
- 锁屏小组件。
- 歌词搜索与逐字歌词。
- 云端同步和跨设备同步。
- 插件市场或第三方扩展系统。
- Windows 或 Linux 版本。

---

## 5. 交互模型

### 5.1 状态定义

| 状态 | 触发条件 | 表现 | 退出条件 |
|---|---|---|---|
| S0 静默收起 | 无活动 | 与物理刘海融为一体 | 出现活动、悬停、点击或拖拽 |
| S1 紧凑活动 | 音乐播放、计时器运行 | 刘海左右显示必要信息 | 活动结束、悬停或点击 |
| S2 临时 HUD | 充电、低电量、音量/亮度变化 | 短暂显示图标、数值和进度 | 1.5 秒后自动消失 |
| S3 悬停预览 | 指针停留达到阈值 | 展示轻量摘要，不夺取键盘焦点 | 指针离开或点击 |
| S4 两翼快捷控制 | 点击刘海、向下手势 | 在刘海左右两翼显示高频按钮，不向下展开大浮层 | 点击外部、Esc、向上手势 |
| S5 文件接收 | 文件进入刘海热区 | 扩大接收范围，显示“松手暂存” | 放下文件或拖离热区 |
| S6 普通功能窗口 | 打开文件架、完整播放器等复杂功能 | 打开可移动、可关闭且不置顶的普通 macOS 窗口 | 关闭或最小化窗口 |

### 5.2 状态优先级

从高到低：

1. 文件正在拖入。
2. 低电量等重要系统状态。
3. 用户正在操作的两翼快捷控制。
4. 即将结束或正在响铃的计时器。
5. 正在播放的音乐。
6. 普通临时系统 HUD。
7. 静默状态。

规则：

- 用户正在操作两翼快捷控制时，普通 HUD 不得强制替换当前内容，只在边缘提示或排队显示。
- 文件拖入必须立即获得最高视觉反馈，但不得中断实际音乐播放或计时器运行。
- 同时存在音乐和计时器时，左右两侧分别显示；空间不足时优先保留计时器。
- 临时 HUD 消失后，恢复此前的音乐或计时器状态。

### 5.3 基础手势与鼠标行为

| 操作 | 结果 |
|---|---|
| 指针进入刘海区域并停留 180ms | 打开悬停预览 |
| 单击刘海 | 打开两翼快捷控制 |
| 指针离开预览区域 400ms | 自动收起 |
| 点击面板外部 | 收起面板 |
| 按 Esc | 收起面板 |
| 双指向下滑 | 打开两翼快捷控制，可在设置中关闭 |
| 双指向上滑 | 收起两翼快捷控制 |
| 文件进入顶部中央热区 | 切换到文件接收状态 |
| 右键刘海 | 打开快捷菜单：设置、暂停、退出 |

### 5.4 减少动态效果

当系统开启“减少动态效果”时：

- 禁用拉伸、果冻和弹性过冲。
- 使用 150–200ms 淡入淡出和尺寸过渡。
- 功能、状态顺序和信息内容保持不变。

---

## 6. 界面与尺寸规范

所有尺寸均使用 macOS points，不直接使用物理像素。

### 6.1 物理刘海检测

优先使用 `NSScreen.safeAreaInsets`、`auxiliaryTopLeftArea` 和 `auxiliaryTopRightArea` 获取屏幕顶部安全区域。

物理刘海尺寸计算：

```text
notchWidth = screenFrame.width - leftAuxiliaryArea.width - rightAuxiliaryArea.width
notchHeight = leftAuxiliaryArea.height
```

若获取不到有效刘海尺寸，则进入无刘海悬浮胶囊模式，不根据机型名称硬编码。

### 6.2 收起状态

#### 带物理刘海显示器

- 基础宽度：实际 `notchWidth`。
- 基础高度：实际 `notchHeight`。
- 左侧信息翼：44–72pt，根据内容自适应。
- 右侧信息翼：44–72pt，根据内容自适应。
- 内容与物理刘海之间不增加无意义空隙。
- 底部圆角：建议 16–18pt。
- 与顶部连接的肩部圆角：8–12pt，视觉上与菜单栏融合。

#### 无刘海显示器

- 默认胶囊尺寸：230 × 36.67pt。
- 内容较少时允许缩短，但高度保持 36–38pt。
- 圆角为高度的一半，形成完整胶囊。
- 距离屏幕顶部：6pt，用户可在设置中调整为 0–12pt。

### 6.3 两翼快捷控制状态

- 带刘海屏默认宽度：实际 `notchWidth + 152pt`，即左右各增加 76pt 操作翼。
- 带刘海屏默认高度：实际 `notchHeight + 7pt`，与静默态保持同一底部点击热区，不向下侵入应用内容。
- 无刘海显示器默认尺寸：320 × 52pt。
- 操作内容只放在物理刘海左右两翼，物理摄像头区域不放文字或按钮。
- 每侧只保留 1–2 个最高频按钮；完整信息与复杂操作进入普通功能窗口。
- 外部圆角：带刘海屏 18pt；无刘海屏为高度的一半。
- S1 紧凑活动、S2 临时 HUD、S3 悬停预览、S4 两翼控制和 S5 文件接收在物理刘海屏上统一保持 `notchHeight + 7pt` 高度，只允许横向改变宽度。

### 6.4 刘海内容分区

沿用苹果灵动岛的左右信息语义，但服从 Mac 菜单栏空间限制：

| 区域 | 用途 |
|---|---|
| Leading | 功能身份、封面、状态图标 |
| Trailing | 数值、倒计时、进度、关键状态 |
| Center | 物理摄像头区域，不放可见内容 |
| Bottom | 不在刘海中设置；复杂内容进入普通功能窗口 |

### 6.5 普通功能窗口

文件架、完整播放器、详细计时器和设置使用普通 macOS 窗口承载。

- 首版默认尺寸：420 × 340pt，最小尺寸 360 × 280pt。
- 窗口可移动、可调整大小、可最小化和关闭。
- 窗口层级为普通层级，不始终置顶，不跟随刘海覆盖所有 Space。
- 记住用户最后一次窗口位置和尺寸。
- 不通过辅助功能权限强制移动或缩放其他应用窗口。

### 6.6 视觉规范

- 主背景：纯黑或接近 OLED 黑的 `#000000`。
- 主文字：白色，主要信息不低于 90% 不透明度。
- 次要文字：白色 55%–70% 不透明度。
- 强调色：优先跟随内容，如专辑封面主色；无内容时使用系统强调色。
- 图标：优先使用 SF Symbols。
- 紧凑态图标：18–22pt。
- 两翼快捷控制图标：18–22pt，点击区域不小于 32 × 32pt。
- 普通功能窗口中的主要图标/封面：44–56pt。
- 不使用持续高频闪烁或无意义循环动画。

### 6.7 动画建议值

- 收起到展开：弹簧响应 0.38–0.45 秒，阻尼 0.78–0.86。
- 临时 HUD 出现：180–240ms。
- 临时 HUD 停留：默认 1.5 秒。
- 内容切换：160–220ms，尺寸和透明度同步变化。
- 文件拖入：接收区在 100ms 内给出视觉响应。
- 动画中保持内容中心稳定，避免文字先移动、背景后追赶。

---

## 7. 功能需求

### 7.1 音乐控制

#### 7.1.1 功能目标

让用户不切换应用即可看见当前播放内容并完成最常用的播放控制。

#### 7.1.2 功能范围

| 编号 | 需求 | 优先级 |
|---|---|---|
| MUS-01 | 自动识别当前受支持播放器的播放状态 | P0 |
| MUS-02 | 显示专辑封面、歌曲名和艺人 | P0 |
| MUS-03 | 播放/暂停 | P0 |
| MUS-04 | 上一首/下一首 | P0 |
| MUS-05 | 显示播放进度和总时长 | P0 |
| MUS-06 | 在展开面板中拖动播放进度 | P0，能力允许时 |
| MUS-07 | 点击歌曲信息跳转到来源播放器 | P0 |
| MUS-08 | 根据封面提取强调色 | P1 |
| MUS-09 | 音频可视化 | P1 |

#### 7.1.3 首版播放器范围

1. Apple Music。
2. Spotify。
3. 其他支持系统媒体信息的播放器作为实验兼容项，不承诺首版完整控制。

#### 7.1.4 界面表现

- 紧凑态左侧显示 24pt 封面，右侧显示播放状态或简化进度。
- 悬停预览显示歌曲名、艺人、播放/暂停按钮。
- 两翼快捷控制保留上一首、播放/暂停和下一首，并提供进入完整音乐窗口的按钮。
- 48–56pt 封面、歌曲信息、完整进度条和三枚控制按钮放入普通功能窗口。
- 音乐暂停超过 10 分钟且没有其他活动时，回到静默收起状态。

#### 7.1.5 异常处理

- 播放器退出：清除音乐活动并恢复其他状态。
- 封面缺失：显示来源应用图标，不显示破图。
- 同时存在多个播放器：优先显示最近发生播放状态变化的播放器。
- 无法拖动进度：进度条退化为只读，不影响播放/暂停和切歌。

#### 7.1.6 技术风险与降级方案

macOS 没有一个覆盖所有播放器且长期稳定的统一公开 Now Playing 控制接口。首版采用播放器适配层：

- Apple Music 与 Spotify 使用各自可用的脚本或公开控制能力。
- 通用系统媒体适配器与业务 UI 解耦，可单独替换。
- 若某个来源无法稳定读取进度，则只提供能可靠实现的控制项。
- 不让单一播放器适配失败阻塞整个刘海应用。

---

### 7.2 文件暂存与 AirDrop

#### 7.2.1 功能目标

让刘海成为一个随时可见的临时文件中转站，而不是长期文件管理器。

#### 7.2.2 功能范围

| 编号 | 需求 | 优先级 |
|---|---|---|
| FIL-01 | 支持从 Finder 和其他应用拖入文件 | P0 |
| FIL-02 | 文件进入热区时立即显示接收反馈 | P0 |
| FIL-03 | 将文件复制到应用本地暂存目录，不移动原文件 | P0 |
| FIL-04 | 显示文件名、类型、大小和缩略图 | P0 |
| FIL-05 | 支持从文件架拖出文件 | P0 |
| FIL-06 | 单击打开文件 | P0 |
| FIL-07 | 单个删除和全部清空 | P0 |
| FIL-08 | 通过系统 AirDrop 面板发送一个或多个文件 | P0 |
| FIL-09 | 默认 24 小时后自动清理暂存副本 | P0 |
| FIL-10 | 修改保留时间 | P1 |

#### 7.2.3 文件处理规则

- 默认只接收普通文件和文件夹，不执行任何拖入内容。
- 拖入操作复制文件，绝不删除或移动源文件。
- 同名文件使用时间或序号区分，不静默覆盖。
- 默认暂存位置位于应用容器/Application Support 目录。
- 自动清理只删除应用创建的暂存副本，不触碰源文件。
- 用户主动清空前显示明确的清理范围，不使用“清空所有文件”等容易误解的文案。
- 单文件大小超过 2GB 时显示空间提醒，但允许用户继续。
- 磁盘空间不足时终止复制并说明原因，不留下显示为成功的残缺文件。

#### 7.2.4 AirDrop

- 使用系统 `NSSharingService.Name.sendViaAirDrop` 打开原生 AirDrop 分享流程。
- 未开启 AirDrop 或当前不可用时，保留文件并提示用户检查系统设置。
- AirDrop 失败不删除暂存文件。

#### 7.2.5 沙盒与持久访问

- 文件拖入后优先复制到应用容器，减少对外部文件长期权限的依赖。
- 若后续功能需要持久引用源文件，使用 security-scoped bookmark，不保存失效的裸路径作为唯一引用。
- 文件访问结束后及时释放 security-scoped resource。

---

### 7.3 系统状态

#### 7.3.1 功能目标

用与刘海融合的方式反馈关键系统变化，减少对系统原有大面积 HUD 的依赖，但首版不强制替换系统 HUD。

#### 7.3.2 功能范围

| 编号 | 需求 | 优先级 |
|---|---|---|
| SYS-01 | 电源接入/断开提示 | P0 |
| SYS-02 | 显示当前电量与充电状态 | P0 |
| SYS-03 | 低电量提醒 | P0 |
| SYS-04 | 音量变化提示和数值 | P0 |
| SYS-05 | 静音状态提示 | P0 |
| SYS-06 | 屏幕亮度变化提示和数值 | P0，技术验证项 |
| SYS-07 | 键盘背光提示 | P1 |
| SYS-08 | 蓝牙设备连接状态 | P1 |

#### 7.3.3 展示规则

- 普通变化展示 1.5 秒。
- 低电量等重要状态停留 3 秒，并允许用户点击查看详情。
- 连续快速调整音量或亮度时只更新当前 HUD，不重复入场动画。
- 数值变化使用平滑进度，不播放音效。
- 在全屏演示、视频播放或屏幕共享时，遵循用户的“全屏显示”设置。

#### 7.3.4 权限与技术降级

- 电池和电源状态不要求额外用户权限。
- 音量状态优先使用 Core Audio 属性监听。
- 亮度变化监听需要先完成公开 API 与系统版本兼容性验证。
- 如果亮度或按键拦截只能依赖辅助功能权限，则采用渐进授权，用户拒绝后隐藏对应功能，不影响音乐、文件和计时器。
- 首版默认与系统 HUD 共存；只有在稳定、可恢复且用户主动开启时，才尝试替换系统 HUD。

---

### 7.4 计时器

#### 7.4.1 功能目标

提供无需打开独立应用的快速倒计时。

#### 7.4.2 功能范围

| 编号 | 需求 | 优先级 |
|---|---|---|
| TIM-01 | 快速选择 5、10、15、25、30、60 分钟 | P0 |
| TIM-02 | 自定义分钟和秒 | P0 |
| TIM-03 | 启动、暂停、继续和结束 | P0 |
| TIM-04 | 紧凑态显示剩余时间 | P0 |
| TIM-05 | 结束时显示动画和系统通知 | P0 |
| TIM-06 | 睡眠唤醒后自动校正剩余时间 | P0 |
| TIM-07 | 同时运行多个计时器 | P1 |

#### 7.4.3 界面表现

- 紧凑态右侧使用等宽数字显示 `MM:SS`。
- 剩余 60 秒时使用强调色，但不持续闪烁。
- 两翼快捷控制显示剩余时间和暂停按钮；完整进度与结束操作放入普通功能窗口。
- 结束后保持提醒状态，直到用户确认或 30 秒后自动收起。

#### 7.4.4 计时规则

- 记录目标结束时间，不依赖每秒累加计数，避免睡眠和系统调度导致漂移。
- 应用重启后恢复尚未结束的计时器。
- 系统时间发生显著变化时重新计算，并显示一次校正提示。

---

### 7.5 设置与菜单栏

#### 7.5.1 设置项

| 分组 | 设置项 |
|---|---|
| 通用 | 开机启动、菜单栏图标显示、暂停 NotchFlow、退出应用 |
| 交互 | 悬停开启、悬停延迟、手势开启、点击外部收起 |
| 显示 | 目标显示器、全屏时显示、外接屏顶部间距、尺寸微调 |
| 功能 | 音乐、文件架、系统状态、计时器独立开关 |
| 文件架 | 自动清理、查看占用空间、立即清空 |
| 动画 | 标准、柔和、快速；跟随系统减少动态效果 |
| 权限 | 展示各权限状态并提供跳转系统设置入口 |

#### 7.5.2 菜单栏快捷菜单

- 打开设置。
- 展开/收起刘海。
- 暂停 1 小时。
- 暂停至明天。
- 退出应用。

---

## 8. 多显示器、全屏与窗口行为

### 8.1 多显示器

- 默认显示在内置 MacBook 屏幕。
- 用户可选择“内置屏幕”“主显示器”或指定显示器。
- 显示器插拔后重新计算位置和安全区域，不依赖旧坐标。
- 目标为无刘海屏幕时自动使用悬浮胶囊模式。
- 合盖后自动迁移到主外接显示器。

### 8.2 全屏

- 默认在全屏应用中隐藏，避免遮挡视频和演示。
- 用户可选择“始终显示”“仅显示重要提醒”或“完全隐藏”。
- 全屏状态变化后不应出现窗口闪烁或落在错误 Space。

### 8.3 窗口规则

- 面板无标题栏、无 Dock 图标、背景透明。
- 收起和临时活动不主动抢占键盘焦点。
- 展开后仅在需要键盘输入时激活应用。
- 支持所有 Space，但设置窗口使用普通窗口层级，可跨显示器移动。
- 点击穿透仅对透明且不可交互区域生效。

---

## 9. 权限设计

### 9.1 权限原则

1. 首次启动不一次性索取全部权限。
2. 用户首次使用对应功能时再申请。
3. 权限申请前先说明用途、影响和拒绝后的结果。
4. 拒绝某项权限只关闭相关功能，不阻断整个应用。
5. 设置页持续显示权限状态和修复入口。

### 9.2 权限矩阵

| 权限/能力 | 用途 | 是否首启申请 | 拒绝后的表现 |
|---|---|---|---|
| 辅助功能 | 增强系统按键/HUD 监听 | 否 | 保留电池等无需权限的状态，关闭增强 HUD |
| 自动化/Apple Events | 控制 Apple Music、Spotify | 首次使用播放器时 | 音乐来源不可控，其他功能正常 |
| 通知 | 计时结束提醒 | 首次启动计时器时 | 仅在刘海内提醒 |
| 用户选择文件 | 文件拖入与导出 | 按操作触发 | 取消本次操作 |
| 开机启动 | 登录后自动运行 | 用户主动开启时 | 不自动启动 |

---

## 10. 首次启动与空状态

### 10.1 首次启动流程

1. 展示一句话价值：让 MacBook 刘海成为随手可用的快捷空间。
2. 演示悬停、点击和拖文件三个基本动作。
3. 默认开启音乐、文件架、系统状态和计时器。
4. 不在此阶段请求辅助功能或自动化权限。
5. 完成后回到静默刘海状态，并在菜单栏保留设置入口。

### 10.2 空状态

- 没有音乐、文件和计时器时，保持与物理刘海融合。
- 外接无刘海显示器上，允许用户选择“始终显示小胶囊”或“完全隐藏”。
- 文件架为空时显示拖拽提示，不展示复杂插画。

---

## 11. 异常与恢复

| 异常 | 处理要求 |
|---|---|
| 显示器拔出 | 面板迁移到可用主显示器，不留在屏幕外 |
| 屏幕分辨率变化 | 立即重新计算几何位置 |
| 应用崩溃后重启 | 恢复计时器和已完成复制的暂存文件 |
| 音乐来源失联 | 结束音乐活动，不影响其他模块 |
| 文件复制失败 | 明确提示原因，不显示为已暂存 |
| AirDrop 不可用 | 保留文件并引导检查系统设置 |
| 权限被系统撤回 | 更新设置页状态，并降级相关功能 |
| 动画卡顿 | 优先保证点击和状态正确，可降低模糊与动画复杂度 |
| 无法识别物理刘海 | 自动使用顶部悬浮胶囊，不显示错位刘海 |

---

## 12. 非功能要求

### 12.1 性能

- 冷启动目标：2 秒内进入可用状态。
- 静默状态 CPU 平均占用目标：低于 1%。
- 静默状态内存目标：低于 100MB。
- 普通动画保持 60fps；高刷新率屏幕尽可能跟随系统刷新率。
- 音乐封面、文件缩略图和模糊效果必须异步处理，不阻塞主线程。
- 没有活动时停止不必要的高频轮询。

### 12.2 稳定性

- 单个功能模块异常不得导致整个面板消失或崩溃。
- 窗口位置、计时器、文件索引和设置需要持久保存。
- 暂存索引与实际文件不一致时，以磁盘真实状态为准并自动修复索引。

### 12.3 隐私与安全

- 不采集行为分析数据。
- 不上传音乐信息、文件信息或系统状态。
- 不内置第三方广告或追踪 SDK。
- 文件只存储在本地应用目录。
- 不读取拖入范围之外的文件。
- 不保存 Apple Music、Spotify 或系统账号密码。

### 12.4 可访问性

- 所有按钮提供 VoiceOver 标签。
- 颜色不是状态区分的唯一方式。
- 支持系统减少动态效果和增强对比度。
- 关键文本不低于 11pt，主要信息不低于 13pt。
- 键盘可完成展开、切换控制项和关闭面板。

---

## 13. 技术架构建议

### 13.1 分层

```text
App Shell
├── Display & Window Layer
│   ├── ScreenGeometryService
│   ├── NotchPanelController
│   └── MultiDisplayCoordinator
├── Interaction Engine
│   ├── IslandStateMachine
│   ├── ActivityPriorityQueue
│   └── GestureAndPointerMonitor
├── Feature Modules
│   ├── NowPlaying
│   ├── FileShelf
│   ├── SystemHUD
│   └── Timer
├── Shared UI
│   ├── IslandShape
│   ├── RegionLayout
│   ├── MotionSystem
│   └── Accessibility
└── Local Data
    ├── SettingsStore
    ├── ShelfRepository
    └── TimerRepository
```

### 13.2 关键技术原则

- 状态机与具体功能解耦，新增活动不直接修改窗口核心逻辑。
- 所有功能通过统一 Activity 协议提供优先级、紧凑视图、展开视图和生命周期。
- 窗口几何只由 ScreenGeometryService 计算，业务模块不得自行读取屏幕坐标。
- 音乐播放器通过 Adapter 协议接入，避免绑定单一私有实现。
- 文件架复制和清理操作在后台队列执行，UI 只消费进度和结果。
- 不依赖机型白名单判断是否有刘海。

### 13.3 建议活动接口

```swift
protocol IslandActivity {
    var id: String { get }
    var priority: Int { get }
    var presentation: IslandPresentation { get }
    var autoDismissAfter: Duration? { get }

    associatedtype CompactContent: View
    associatedtype ExpandedContent: View

    @ViewBuilder func compactContent() -> CompactContent
    @ViewBuilder func expandedContent() -> ExpandedContent
}
```

该接口仅表达方向，最终实现可使用类型擦除解决多模块注册问题。

---

## 14. MVP 验收标准

### 14.1 整体验收

- [ ] 应用启动后准确显示在目标屏幕顶部中央。
- [ ] 带刘海和无刘海显示器均无明显错位。
- [ ] 收起、悬停、展开、点击外部收起形成完整闭环。
- [ ] 四个核心功能可独立关闭，关闭后不继续请求相关权限或执行轮询。
- [ ] 多个活动同时存在时符合既定优先级，不随机覆盖。
- [ ] 开启“减少动态效果”后不再出现弹性和拉伸动画。

### 14.2 音乐验收

- [ ] Apple Music 和 Spotify 可识别播放/暂停状态。
- [ ] 能显示歌曲名、艺人以及可用时的封面。
- [ ] 播放/暂停、上一首和下一首有效。
- [ ] 来源退出或权限被拒绝时不会导致应用崩溃。

### 14.3 文件架验收

- [ ] 从 Finder 拖入文件后 100ms 内出现接收反馈。
- [ ] 放下后复制成功，源文件位置和内容不改变。
- [ ] 暂存文件可打开、拖出、删除和 AirDrop。
- [ ] 应用重启后文件索引仍正确。
- [ ] 自动清理只删除应用暂存副本。
- [ ] 磁盘空间不足或权限失败时有明确反馈。

### 14.4 系统状态验收

- [ ] 电源接入、断开和低电量状态显示准确。
- [ ] 连续调整音量时 HUD 原地更新，不重复闪烁。
- [ ] 用户拒绝辅助功能权限后，其他功能仍正常。
- [ ] 亮度 HUD 若无法稳定实现，设置页明确标为不可用，不显示错误数值。

### 14.5 计时器验收

- [ ] 可启动、暂停、继续和取消计时器。
- [ ] 系统睡眠唤醒后剩余时间准确。
- [ ] 应用重启后能恢复未结束计时器。
- [ ] 计时结束后刘海内有明确提醒；通知权限可选。

### 14.6 显示器与全屏验收

- [ ] 插拔显示器后面板不会留在屏幕外。
- [ ] 合盖模式可迁移至主外接显示器。
- [ ] 设置窗口可在不同显示器之间正常移动。
- [ ] 全屏隐藏策略与用户设置一致。

---

## 15. 测试矩阵

| 维度 | 至少覆盖 |
|---|---|
| 系统 | macOS 14、15、当前开发环境版本 |
| 屏幕 | 14 英寸刘海屏、16 英寸刘海屏、无刘海外接屏 |
| 显示模式 | 单屏、扩展屏、镜像、合盖模式 |
| Space | 普通桌面、全屏应用、多 Space 切换 |
| 播放器 | Apple Music、Spotify、播放器退出、权限拒绝 |
| 文件 | 小文件、大文件、文件夹、同名文件、磁盘空间不足 |
| 辅助功能 | 已授权、拒绝、授权后撤回 |
| 动效 | 标准、减少动态效果、增强对比度 |

---

## 16. 开发阶段

### 阶段 0：技术验证

- 物理刘海尺寸计算。
- `NSPanel` 在菜单栏、Space、全屏和多显示器中的稳定性。
- Apple Music/Spotify 控制能力。
- 音量、亮度和电源状态监听能力。
- 文件拖入、沙盒复制、拖出和 AirDrop。

退出条件：所有 P0 能力得到“可实现、需要降级或需要移出首版”的明确结论。

### 阶段 1：交互底座

- 窗口与屏幕几何。
- 状态机和优先级队列。
- 收起、临时活动、悬停、两翼快捷控制和普通功能窗口。
- 动画系统与减少动态效果。

### 阶段 2：四个核心模块

- 音乐。
- 文件架/AirDrop。
- 系统状态。
- 计时器。

### 阶段 3：设置、权限与恢复

- 设置页和菜单栏。
- 渐进权限申请。
- 数据持久化和异常恢复。
- 多显示器、全屏和合盖模式。

### 阶段 4：验收与发布准备

- 按测试矩阵回归。
- 性能与内存优化。
- 代码签名、公证和 DMG。
- 隐私说明、首次启动引导和版本说明。

---

## 17. 已知风险与控制方案

| 风险 | 影响 | 控制方案 |
|---|---|---|
| 通用 Now Playing 接口不稳定 | 部分播放器无法读取或控制 | 播放器适配层；首版明确支持 Apple Music/Spotify；按能力降级 |
| 亮度监听依赖系统版本或非公开能力 | 亮度 HUD 可能无法稳定发布 | 阶段 0 验证；不能保证准确时不显示错误值 |
| `NSPanel` 与全屏/Space 冲突 | 面板错位、无法点击或遮挡视频 | 窗口层与设置窗口分离；建立多显示器回归矩阵 |
| 菜单栏管理工具改变可用区域 | 刘海两侧内容被遮挡 | 以实际屏幕安全区和用户尺寸设置为准，避免固定坐标 |
| 文件沙盒权限失效 | 重启后无法访问源文件 | 文件复制进应用容器；必要时使用 security-scoped bookmark |
| 拖入大文件占用磁盘 | 暂存目录膨胀 | 显示大小、默认 24 小时清理、提供一键清空 |
| 参考 GPL 项目产生许可风险 | 影响闭源或商业分发 | 只参考产品体验；实现保持独立；代码优先参考 MIT 与苹果公开文档 |

---

## 18. 后续版本候选

以下内容必须在 MVP 稳定后重新评估，不自动进入下一版：

1. 日历与下一个日程。
2. 剪贴板历史。
3. 天气。
4. AI 快捷命令。
5. 通知镜像。
6. 歌词和音频可视化。
7. 蓝牙设备状态。
8. 多计时器。
9. 第三方活动扩展 API。

评估标准：是否高频、是否适合顶部短时交互、是否需要敏感权限、是否会让普通功能窗口变复杂。

---

## 19. 参考规范与技术资料

- Apple Human Interface Guidelines — Live Activities：  
  https://developer.apple.com/design/human-interface-guidelines/live-activities
- Apple Developer — `NSScreen.auxiliaryTopLeftArea`：  
  https://developer.apple.com/documentation/appkit/nsscreen/auxiliarytopleftarea-uglc
- Apple Developer — `NSSharingService`：  
  https://developer.apple.com/documentation/appkit/nssharingservice
- Apple Developer — macOS App Sandbox 文件访问：  
  https://developer.apple.com/documentation/security/accessing-files-from-the-macos-app-sandbox
- MIT 开源参考 `gh-notch`：  
  https://github.com/aymandakirgh/ghnotch
- MIT 开源参考 `NotchDrop`：  
  https://github.com/Lakr233/NotchDrop
- 交互研究参考 `DynamicNotch`（GPL-3.0，仅研究体验与架构思想）：  
  https://github.com/jackson-storm/DynamicNotch

---

## 20. PRD 完成定义

本 PRD 已明确：

- 产品定位与首版边界。
- 四个核心功能及其降级策略。
- 灵动岛状态、优先级和关键交互。
- 带刘海与无刘海显示器的尺寸规范。
- 权限、隐私、性能和异常要求。
- 开发分层、阶段安排和可执行验收标准。

进入 UI 原型和阶段 0 技术验证时，不再重新讨论已确认的核心范围；只有验证结果证明某项能力无法稳定实现时，才按本文的降级策略调整。
