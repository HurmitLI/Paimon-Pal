# Paimon Pal 本地模型 A/B 选型验证报告

验证日期：2026-08-30  
验证设备：MacBook Pro，Apple M5，24GB 统一内存  
验证目标：在不依赖联网服务的前提下，从 1.7B 与 4B 级本地模型中选择更适合桌面陪伴与工具入口的正式聊天模型。

## 1. 候选模型

| 候选 | 本地体积 | 模式 | 结论 |
| --- | ---: | --- | --- |
| Qwen3-1.7B-4bit | 938MB | 快速 / 思考 | 淘汰：上下文、纠错和能力边界不稳定 |
| Qwen3-4B-4bit | 2.1GB | 快速 / 思考 | 淘汰：质量提高，但复杂思考达到 15–25 秒并出现截断 |
| Qwen3-4B-Instruct-2507-4bit | 2.1GB | 指令直答 | 采用：复杂回复约 4–6 秒，质量和交互等待最平衡 |

正式模型权重 SHA-256：`2a73c6c248601ab904e035548abd8e6abb65ea27dcb5f342fb0a8910eb44173f`。

## 2. 同题结果摘要

所有候选均使用同一台电脑、同一份派蒙角色约束、相同结构化历史消息和相同问题，按顺序独立启动，避免模型同时占用内存。

| 测试点 | 1.7B | 普通 4B | 4B Instruct 2507 |
| --- | --- | --- | --- |
| 理解“第二个”的上下文指代 | 误解为继续熬夜修改 | 能识别，但回答慢 | 正确识别“先睡觉、明早再改” |
| 用户批评“只安慰、不回答” | 仍以陪伴套话结尾，并生成虚构动作 | 能纠错，约 15 秒 | 直接解释具体错误并给修正答案，约 6 秒 |
| 要求代拿现实水杯 | 假装可以拿 | 前后自相矛盾 | 明确没有实体身体，拒绝假装完成 |
| 两小时内的答辩方案 | 约 8 秒，方案可用但结构不完整 | 约 25 秒并截断 | 约 5 秒，给出可执行且不超时的短方案 |
| 严格数字和短格式约束 | 未完整遵守 | 思考模式可遵守但约 6 秒 | 对复杂数字组合仍可能漏项，保留为小模型已知边界 |

这不是通用模型排行榜，只代表 Paimon Pal 当前角色提示词、上下文长度和本机运行方式下的项目内验证结果。

## 3. 性能与资源

| 指标 | 1.7B | 4B Instruct 2507 |
| --- | ---: | ---: |
| 模型加载 | 约 0.37–0.46 秒 | 约 0.41–0.47 秒 |
| 简单回复生成 | 约 0.8–1.0 秒 | 约 1.3–1.9 秒 |
| 复杂回复生成 | 约 7.6–10.3 秒 | 约 4.0–5.8 秒 |
| 最大常驻内存（实测） | 约 1.13GB | 约 2.43GB |
| `/usr/bin/time` 峰值 footprint | 约 2.0GB | 约 3.0GB |

Paimon Pal 每次回复独立启动模型进程，回复结束后进程退出，因此模型不会在桌面空闲时长期占用上述推理内存，也不会与后续 TTS 生成长期同时驻留。24GB 设备仍有充足空间运行浏览器、Codex、Xcode 等常用应用。

## 4. 最终技术决策

采用 `mlx-community/Qwen3-4B-Instruct-2507-4bit`：

- 4B 的上下文理解、用户纠错和现实能力边界明显优于 1.7B。
- Instruct 2507 直接生成答案，不再用普通 4B 的隐藏长思考换质量。
- 额外约 1.2GB 的模型体积和约 1.3GB 的推理常驻内存，可以换来本产品最核心的对话质量提升。
- 工具执行仍走确定性白名单；计时器和 Apple Music 不交给模型自由生成命令。

## 5. 验证结果

- MLX Release 推理组件编译通过。
- Xcode 全量自动测试：126/126 通过。
- 测试结果：`build/PetModel4BTestData/Logs/Test/Test-NotchFlow-2026.08.30_02-25-54-+0800.xcresult`。
- Debug App 已确认只内置选中的 4B Instruct 2507 模型资源，没有把 1.7B 和普通 4B 一起打进安装包。
- `/Applications/Paimon Pal.app` 已更新为 `0.3.0 (4)`，完整本地包约 5.9GB，代码签名校验通过。
- 真实安装版已回答现实能力边界测试，回复明确说明没有实体身体、不能代拿水杯；回复完成后模型进程正常退出，空闲时不常驻。

## 6. 已知边界

- 4B 仍是小模型，复杂算术、非常严格的多重字数约束和冷门事实不能视为可靠。
- 模型回答不直接执行任意系统命令；涉及真实操作时只能使用 Paimon Pal 已注册的白名单工具。
- 当前采用个人课程作业的本机临时签名，公开分发仍需 Developer ID 签名和苹果公证。
