# NotchFlow 第四阶段开发段 4.3 验证报告｜显示器、全屏与合盖

> 文档版本：v1.0  
> 验证日期：2026-08-27  
> 当前结论：本机全屏人工出口通过；外接屏与合盖待真实设备补验  
> 当前分支：`phase/04-settings-permissions-recovery`

---

## 1. 本段完成内容

- 设置新增“显示”分页。
- 目标屏幕支持“内置屏幕 / 主显示器 / 指定显示器”。
- 使用系统显示器 UUID 保存指定目标，不依赖会变化的屏幕数组位置。
- 内置屏不可用、指定屏拔出或合盖后，临时回退到当前主显示器；原选择不被清除，设备回来后可自动恢复。
- 无物理刘海的显示器使用顶部悬浮胶囊，并支持顶部间距 `0...40 pt`、宽度 `-40...+80 pt` 微调。
- 全屏支持“始终显示 / 仅显示重要提醒 / 完全隐藏”，默认“完全隐藏”。
- “仅显示重要提醒”只保留严重系统提醒、计时结束和文件接收。
- 监听屏幕参数变化、Space 切换和前台应用变化，重新计算位置与可见性。

## 2. 自动验证

执行命令：

```bash
xcodebuild test -project NotchFlow.xcodeproj -scheme NotchFlow \
  -destination 'platform=macOS' \
  -derivedDataPath build/NotchFlowPhase43Data \
  -resultBundlePath /tmp/NotchFlow-Phase4-4.3-final2-20260827.xcresult
```

结果：

- 总测试数：57
- 通过：57
- 失败：0
- 跳过：0
- `git diff --check`：通过

新增覆盖：

- 显示设置默认值、持久化、非法值回退和范围约束。
- 内置屏、主显示器、指定显示器的选择逻辑。
- 内置屏/指定屏消失后回退主显示器。
- 当前 Mac 至少能解析一个真实屏幕。
- 无刘海外接屏间距与宽度微调计算。
- 全屏窗口边界识别容差。
- 三档全屏可见性策略和重要提醒白名单。

## 3. 当前 Mac 运行检查

- 当前前台窗口不是全屏时，默认“完全隐藏”不会误隐藏刘海。
- 物理刘海静默面板真实窗口：`209 × 39`，坐标位于屏幕顶边。
- Debug 设置窗口真实创建并显示：`600 × 552`。
- 真全屏保留菜单栏时抓取到 `1512 × 949`、顶部保留 `33 pt` 的真实窗口边界；已据此修复全屏识别并增加回归测试。
- 临时 Debug 启动入口已在检查后撤回，正式源码中不存在该入口。
- 应用进程稳定，没有崩溃。

## 4. 尚不能替代的真实设备验证

当前环境只检测到内置 MacBook 屏幕，因此以下结果不能用模拟测试冒充通过：

- 外接无刘海显示器连接、拔出和重新连接。
- 指定外接显示器消失后的主屏回退与重新连接恢复。
- 合盖后迁移到外接主显示器。
- 在真实全屏 Space 中切换三档策略的视觉表现。

这些项目不影响代码完成状态，但必须在具备对应场景时补验。

## 5. 人工出口

先完成本机可执行的全屏验证：

1. 打开 NotchFlow 设置 →“显示”。
2. 确认默认目标为“内置屏幕”，当前目标显示 MacBook 内置屏且标记“有刘海”。
3. 保持全屏行为为“完全隐藏”。
4. 将任意应用进入 macOS 真全屏，确认刘海浮层消失。
5. 退出真全屏，确认刘海浮层自动恢复且位置不跳动。

验收标准：步骤 2、4、5 均符合，且没有残留错位面板。

如果当前没有外接显示器，外接屏与合盖标记为“待真实设备补验”，不阻塞先完成本机全屏出口。

## 6. 人工验收结果

- 产品经理已确认默认内置屏幕与物理刘海位置正常。
- 进入 macOS 真全屏后，NotchFlow 浮层按“完全隐藏”策略消失。
- 退出真全屏后，NotchFlow 收起胶囊自动恢复，且没有残留错位面板。
- 本机可执行出口通过；当前无外接屏，因此外接屏拔插与合盖继续保留为设备补验项。
