# 第六阶段开发段 6.3 技术开发文档：Paimon Pal 动画一致性

## 1. 开发目标

消除派蒙在探头、出场、待机、点击、聆听、说话、成功反馈和钻回动作之间切换时出现的视觉尺寸与位置跳变，让不同来源、不同画布比例的序列帧共享同一角色基准。

## 2. 已确认根因

- 待机动作单帧接近正方形，角色只占画布的一部分。
- 聆听与说话动作单帧为竖向画布，当前等比例适配会把角色显示得更大。
- 现有分状态 `verticalOffset` 只能调整上下位置，不能统一角色实际高度。
- 出场末帧与回程首帧仍采用 444×444 原始画布，而待机已进入 512×512 标准画布，导致回程角色比待机大约 19%。
- 原始“探头/飞出”素材自带近景镜头：刚露头时头部宽度约 245px，而待机动作约 195–197px。即使转场端点已经对齐，仍会在第一瞬间产生“大头”观感。
- 如果只给某一个动作写死缩放系数，后续新增素材仍会重复出现同类问题。

## 3. 本段范围

- 在素材加载层测量清理后图片的有效透明像素边界。
- 对完整身体动作使用统一标准画布、角色高度、脚底锚点和横向中心。
- 同一动作的所有帧共用一个固定缩放和锚点参数，避免逐帧缩放造成呼吸式抖动。
- 待机、点击、聆听、说话和成功反馈纳入标准化。
- 探头、出场和钻回动作也输出 512×512 标准画布，但以相邻动作端点校准比例：探头末帧对齐出场首帧，出场末帧和回程首帧对齐待机。
- 转场动作先使用整段固定变换保留原始位移与遮挡轨迹，再仅对素材自带的镜头变焦应用预设补偿曲线；探头阶段使用 80%，飞出阶段从 80% 平滑恢复到 100%。
- 变焦补偿以当前有效内容的水平中心和脚底为锚点，避免修正头部大小时破坏“从刘海下面钻出”的路径。
- 增加自动测试，确保标准化后各完整身体动作的有效高度和脚底位置处于统一容差内。

## 4. 不进入本段

- 不增加新的动画帧。
- 不改变动作内容、表情或播放速度；仅抵消素材内已有的镜头变焦。
- 不重做完整聊天记录窗口。
- 不接入正式 TTS。

## 5. 验收标准

### 自动验收

- 每个稳定完整身体动作输出同尺寸标准画布。
- 各动作联合有效内容高度差不超过设定容差。
- 各动作脚底锚点差不超过设定容差。
- 稳定完整身体动作的所有帧使用固定变换，不出现逐帧缩放。
- 探头和飞出使用确定性的变焦补偿曲线，刚露头的头部宽度与待机状态处于约 10px 容差内，且脚底锚点不漂移。
- 探头末帧与出场首帧、出场末帧与待机首帧、回程首帧与待机首帧的有效高度和脚底锚点均处于容差内。
- 完整 XCTest 全部通过，开发版构建和单实例启动通过。

### 最终人工验收

用户只需完成一次主观检查：观察“从刘海露头并出场 → 普通待机 → 打开极简输入框进入聆听 → 发送后进入说话 → 回复成功 → 钻回刘海”的完整切换，重点确认刚露头时不再先出现一个偏大的头，并判断全链路角色大小与位置是否自然连续。
