# 第六阶段开发段 6.4 验证报告：Paimon Pal 本地语音正式接入

## 1. 验证结论

开发段 6.4 已全部验收通过。用户选定的 P1“清脆反应感”原创幻想伙伴声线已以完全本地、自包含方式接入 Paimon Pal；对话文字、工具结果和计时结束提醒都可进入同一语音链路。语音生成失败时保留文字和工具结果，不会让主功能失效。

2026-08-30 最终人工听感验收：用户确认正式采用 P1，并同意进入下一开发段。

2026-08-29 首次听感验收发现：开头“旅行者”被明显拖长。已将 A4 语音指令改为称呼必须快速、连贯、短促念完，不得拖音、唱腔或逐字强调；同时仅在 TTS 输入中移除“旅行者”后的紧邻标点，界面回复原文不变。

离线 Whisper 字级对齐结果：长回复中的“旅行者，”由约 0.76 秒缩短为约 0.60 秒，称呼后停顿由约 0.48 秒缩短为约 0.20 秒，同段总语音由 7.60 秒缩短为 6.48 秒。新版安装后仍需用户完成最终听感复验。

2026-08-30 第二次交互验收发现两个问题：语音层会为每条回复强制加上“旅行者”；当用户追问这个现象时，1.7B 小模型又把“旅行者”误当成角色扮演话题。现已取消强制称呼，TTS 只朗读回复原文；同时为这类对助手用词和行为的反馈增加确定性直答，并补充通用反馈规则。

2026-08-30 第三次听感验收发现：不同回复之间的基础声线会变化。根因是 VoiceDesign 会结合每句文本重新生成音色，固定随机种子只能保证同一文本可复现，不能锁定跨文本声纹。现已把用户通过的固定样音改为安装包内参考声纹，后续所有回复均使用该参考音做 ICL 生成，不再逐句重新设计声线。随后用户认为 A4 仍偏通用 AI 感，复选三版更具角色反应感的原创声线并正式选择 P1。

## 2. 已完成内容

- 内置 Python 3.12、MLX Audio、P1 固定参考声纹生成脚本和 `Qwen3-TTS-12Hz-1.7B-VoiceDesign-4bit` 模型。
- 运行时只读取 App Resources，不依赖用户安装 Python，不联网下载模型。
- 文字先显示，音频真正开始播放时才进入派蒙说话动画。
- 新请求会取消旧生成并停止旧播放，不允许多段语音重叠。
- 设置页提供“派蒙语音”开关和“停止播放”入口。
- 语音文本会去除 Markdown、链接和代码块，限制为 160 字，并保持回复原意；不再为每条语音强制添加“旅行者”。
- 用户对助手用词或行为提出质疑时，先直接判断并回应如何调整，不把关键词误当新的角色扮演话题。
- 临时 WAV 在播放结束、打断或下次生成时清理；强制退出遗留文件也会在下次生成前清理。
- 只有本次正在运行的倒计时从 `running` 转为 `ringing` 时才播报；重启恢复旧响铃不重复播报。

## 3. 自包含语音实测

使用最终 App 包内部的 Python、site-packages、生成脚本和模型，脱离项目虚拟环境生成：

- 测试文本：“旅行者，本地语音已经准备好啦。”
- 音频格式：WAVE / PCM 16-bit / mono / 24 kHz。
- 音频时长：3.68 秒。
- 模型加载：0.834 秒。
- 语音生成：2.495 秒。
- 测试进程峰值内存：约 4.643 GB。

## 4. App 内端到端验证

通过 Debug 一次性验收入口，由 App 自动提交“帮我计时 10 秒”：

1. 对话窗口显示用户指令与真实工具结果“好哒，已经开始计时 10 秒。”
2. App 调起包内 `paimon_tts_runtime.py`，生成 226,604 字节 WAV。
3. WAV 在音频播放期间保留，播放结束后自动删除。
4. 10 秒到达后，App 第二次调起包内 TTS，生成并播报“时间到啦！快回来看看吧。”
5. 第二段播放结束后同样删除 WAV，最终无 Python 子进程、无残留 WAV。
6. 验证前特意保留的旧 WAV 在新生成开始时被正确清理。

针对交互反馈又使用最终 `/Applications/Paimon Pal.app` 自动执行两条真实对话：

- 输入“你好像每次跟我对话都会加一个旅行者，是吧？”，界面与 TTS 均收到直接承认和调整说明，未再转成“要一起出发吗”。
- 输入“我今天有点累。”，界面回复与 TTS 入参均为“今天有点累，想休息一下吗？我在这里陪着你～”，开头未被强制添加“旅行者”。
- 两次播放结束后都无残留 TTS 子进程或临时 WAV。

## 5. 自动测试与构建

- XCTest：`110 / 110` 通过，`0` 失败，`0` 跳过。
- 测试结果：`build/NotchFlowFinalTestData/Logs/Test/Test-NotchFlow-2026.08.29_23-45-04-+0800.xcresult`。
- “旅行者”拖音修正后重跑全量回归：`110 / 110` 通过，结果位于 `build/NotchFlowPrefixFixFullTestData/Logs/Test/Test-NotchFlow-2026.08.30_00-01-43-+0800.xcresult`。
- 取消强制称呼与行为反馈直答的针对性测试：`20 / 20` 通过，结果位于 `build/NotchFlowBehaviorFixTestData/Logs/Test/Test-NotchFlow-2026.08.30_00-09-32-+0800.xcresult`。
- 最终全量回归：`111 / 111` 通过，结果位于 `build/NotchFlowBehaviorFixFullTestData/Logs/Test/Test-NotchFlow-2026.08.30_00-11-34-+0800.xcresult`。
- 固定声纹改造后最终全量回归：`111 / 111` 通过，结果位于 `build/NotchFlowFixedVoiceFinalTestData/Logs/Test/Test-NotchFlow-2026.08.30_00-26-30-+0800.xcresult`。
- P1 正式替换后的最终全量回归：`111 / 111` 通过，`0` 失败、`0` 跳过，结果位于 `build/NotchFlowP1FinalTestData/Logs/Test/Test-NotchFlow-2026.08.30_00-39-06-+0800.xcresult`。
- 同一句话使用两个独立 TTS 进程生成，两个 WAV 的 SHA-256 均为 `585d3c3744858a3686b554c13ba727f865d57263b3aac3248975347c5f39af70`。
- 安装包内 `VoiceReference.wav` 与已验收 P1 样音 SHA-256 完全一致，均为 `910568ae9fead5b6996655ab122fde3d2132c1e2296960a0774feb1534f5f20c`。
- 安装版使用安慰、计时确认、打开设置三类文本生成的样音分别为 4.80 秒、3.68 秒和 4.40 秒，均为固定 P1 模式、24 kHz 单声道 WAV，位于 `build/p1-installed-cross-text/`。
- Python 生成脚本编译检查通过。
- 运行时嵌入脚本语法检查和 App 内导入预检通过。
- App 临时签名经 `codesign --verify --deep --strict` 验证通过。
- 最终 App 约 4.7 GB，其中本地 TTS 资源约 3.7 GB。
- 最终 App 只运行 1 个 `NotchFlow` / Paimon Pal 实例。

## 6. 安装结果

- 已安装：`/Applications/Paimon Pal.app`
- 已启动并确认运行的是 `/Applications` 版本。
- 已直接使用 `/Applications/Paimon Pal.app` 内部运行时生成 3.92 秒、24 kHz 验证语音，安装版自包含链路通过。
- 拖音修正后已重新替换安装版；直接用安装包生成的 6.48 秒复听样音与离线对齐验证样音 SHA-256 完全一致。
- 替换前旧版备份：`build/installed-app-backups/Paimon Pal-before-local-tts-20260829-2348.app`
- 拖音修正前的本地语音版备份：`build/installed-app-backups/Paimon Pal-before-prefix-fix-20260830-0003.app`
- 取消强制称呼前的安装版备份：`build/installed-app-backups/Paimon Pal-before-no-forced-traveler-20260830-001340.app`
- 固定 A4 参考声纹前的安装版备份：`build/installed-app-backups/Paimon Pal-before-fixed-voice-20260830-002355.app`
- 固定声纹最终设置文案替换前的安装版备份：`build/installed-app-backups/Paimon Pal-fixed-voice-pre-final-settings-20260830-0028.app`
- P1 正式替换前的完整安装版备份：`build/installed-app-backups/Paimon Pal-before-P1-reference-20260830-004022.app`
- 当前 P1 安装版与上一固定声纹安装版均约 4.7 GB；备份可在本段出现安装问题时恢复。

## 7. 人工听感验收

1. 点击派蒙，输入“我今天有点累”。
2. 确认文字先出现，随后使用你已选的 P1 方向语音播报。
3. 确认真正有声音时派蒙在说话，声音结束后动画自然恢复。

以上项目已通过。开发段 6.4 可以正式封存，并进入现有工具能力扩展阶段。
