# 第六阶段开发段 6.7 技术开发文档：Paimon Pal AI 连续听说实验模式

## 1. 开发目标

为 Paimon Pal 增加一个由用户明确开启的连续听说模式，使派蒙能完成“听到一句话—理解并回复—朗读回复—继续等待下一句”的本地闭环。用户同时开启 Codex 等其他语音助手后，可以用扬声器和麦克风进行有限轮次的 AI 对话实验。

## 2. 本段交付范围

- 菜单栏与设置页提供“AI 对话实验模式”开启/停止入口。
- 使用 macOS Speech 的普通话本机识别，不允许回退到云端转写。
- 复用 Qwen3 4B Instruct 2507 本地模型、现有会话上下文、工具路由和 P1 本地 TTS。
- 派蒙开始朗读前停止收音，朗读结束 0.9 秒后再恢复，避免派蒙听见自己。
- 一句话静默约 1.3 秒后提交；5 秒内完全相同的转写不重复提交。
- 单次最多 12 轮或 10 分钟，可随时停止；暂停应用、关闭派蒙语音或退出应用时强制停止。
- 不保存原始录音，模式不会在重启后自动恢复。

## 3. 状态流转

```text
未开启
  → 请求麦克风/语音识别权限
  → 正在听
  → 正在理解并生成回复
  → 派蒙正在说话（麦克风关闭）
  → 0.9 秒冷却
  → 正在听
```

任意阶段遇到权限拒绝、本机识别不可用、音频输入异常、达到轮次/时长上限或用户主动停止，都会退出本轮实验。

## 4. 与 Codex 的关系

当前实现不是把 Codex API 直接嵌入 Paimon Pal，而是让两个已打开的语音助手通过当前 Mac 的扬声器与麦克风交替听说。因此它适合课程演示和交互实验，不保证在噪声环境下长期稳定，也不允许无人值守无限循环。

## 5. “训练派蒙”的准确边界

AI 互相对话不会直接改变 Qwen3 4B 的模型权重，也不会让模型自动变聪明。它最多能产生候选对话样本。后续若要真正优化，应按以下流程执行：

1. 用户明确同意后，仅保存文字，不保存原始录音。
2. 由 Codex 对候选回复提出改写建议和问题标签。
3. 人工确认哪些回答是正确、自然且符合派蒙角色的。
4. 先把通过样本用于固定评测集和提示词回归。
5. 数据量和质量足够时，再单独制作 LoRA/SFT 数据并训练新版本。

未经人工筛选的 AI 对 AI 对话不能直接进入训练集，否则会放大误识别、套话和错误事实。

## 6. 自动验证范围

- 转写去空白与空文本过滤。
- 5 秒内重复转写抑制。
- 12 轮/10 分钟停止边界。
- 菜单入口在运行时显示“停止”，并显示当前语音阶段。
- 原有菜单、模型聊天、工具、语音和桌宠测试全量回归。

## 7. 必须人工验证的项目

1. 首次开启时，用户亲自允许麦克风与语音识别权限。
2. 对派蒙说一句普通话，确认它能回复、朗读并重新进入“正在听”。
3. 派蒙朗读时确认不会把自己的声音当成下一轮问题。
4. 同时开启 Codex 语音后只做 2 至 3 轮实验，确认双方没有抢话或无限回声。
5. 从菜单栏停止后确认麦克风不再使用，重启 Paimon Pal 后不会自动开启。

## 8. 本段完成标准

- 自动测试与 Release 构建通过。
- 安装版首次权限、单人连续听说和手动停止通过人工验收。
- AI 对 AI 的 2 至 3 轮实验作为增强验收，不作为本地模型训练完成的证明。
