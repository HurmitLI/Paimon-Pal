# Paimon Pal 隐私说明

更新日期：2026-08-29
适用版本：0.3.0 (7)

## 1. 核心原则

Paimon Pal 是纯本地 macOS App：

- 不注册账号。
- 不采集行为分析数据。
- 不上传音乐信息、文件信息、系统状态或计时数据。
- 桌宠对话和模型推理只在本机完成，不发送到云端。
- 不包含广告、第三方追踪 SDK 或云端同步。
- 不保存 Apple Music 或系统账号密码。

## 2. Apple Music

在音乐功能开启且 Apple Music 正在运行时，Paimon Pal 通过 macOS 自动化权限读取当前播放状态、歌曲、歌手、专辑、封面和进度，并发送播放控制命令。这些数据只在本机用于界面显示与控制，不会上传。

拒绝 Apple Music 自动化权限只会影响音乐功能，文件架、系统状态和计时器仍可使用。

## 3. 文件架

Paimon Pal 只访问你主动拖入、通过文件选择器选择或指定导出位置的内容。导入时会在 App Sandbox 的 Application Support 目录创建一份副本；Finder 原文件不会被移动或删除。

暂存副本与索引位于类似以下位置的沙盒目录中：

```text
~/Library/Containers/com.notchflow.app/Data/Library/Application Support/NotchFlow/FileShelf/
```

实际路径由 macOS 沙盒决定。为兼容旧版本，内部目录仍使用 `NotchFlow` 技术名称。暂存副本默认保留 24 小时，也可以提前手动删除。自动清理、单项删除和全部清理只允许处理 Paimon Pal 管理目录内的副本。

## 4. 桌宠与本地模型

桌宠回复由安装包内置的本地模型在当前 Mac 上生成。聊天窗口最多向模型携带最近 8 条本次会话消息；聊天记录不会写入磁盘，退出应用后不会保留。“清空本次对话”会立即移除当前会话上下文。

桌宠可以调用 Paimon Pal 已有的计时器和本地功能窗口，但不会因此获得额外的文件、账号或网络权限。

派蒙语音使用安装包内置的原创 P1 固定参考声线、本地 TTS 模型与 MLX Audio 生成，不调用云端语音服务。待播报文字和参考声线不会上传；临时 WAV 只写入 App 沙盒的 `Application Support/Paimon Pal/Speech/` 目录，播放结束、用户打断或下次生成时会清理。关闭“派蒙语音”只停止生成和播放，不影响文字回复与工具执行。

“AI 对话实验模式”只有在用户主动开启后才会申请麦克风与语音识别权限。音频直接交给 macOS 本机普通话语音识别，原始录音不写入磁盘，也不会在本机识别不可用时回退到联网转写。识别文字只进入当前临时会话；模式最多运行 12 轮或 10 分钟，退出应用后不会自动恢复。

## 5. 系统状态与计时器

- 系统状态只在本机读取电池、电源、音量和静音状态。
- 计时器只在本机保存目标结束时间，用于应用重启或睡眠唤醒后校正。
- 如果允许通知，Paimon Pal 只发送本地计时结束通知。

## 6. 权限用途

| 权限 | 用途 | 拒绝后的影响 |
|---|---|---|
| Apple Music 自动化 | 读取与控制 Apple Music | 仅音乐功能不可用 |
| 通知 | 发送计时结束系统通知 | 刘海内提醒仍可用 |
| 用户选择的文件 | 复制主动选择、拖入或导出的内容 | 只影响对应文件操作 |
| 辅助功能 | 可选的系统 HUD 增强 | 当前核心功能不受影响 |
| 麦克风 | 接收 AI 对话实验模式中的语音 | 不能使用连续听说模式，文字聊天仍可用 |
| 语音识别 | 在本机把普通话转成文字 | 不能使用连续听说模式，文字聊天仍可用 |

打开首次使用说明或设置页不会主动申请上述权限。

## 7. 清理与卸载

- 在文件架中删除项目或选择全部清理，可删除 Paimon Pal 暂存副本。
- 退出 App 不会删除暂存文件，下一次启动会继续执行 24 小时清理规则。
- 卸载 App 前如需彻底清理，可先清空文件架，再删除 App；沙盒残留数据可由用户在对应 Containers 目录中手动移除。
