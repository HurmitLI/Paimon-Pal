# 第六阶段开发段 6.5 技术开发文档：Paimon Pal 工具能力扩展

## 1. 开发目标

让派蒙不仅能开始计时和打开功能窗口，还能直接控制已经属于 Paimon Pal 的计时器和 Apple Music 能力。所有操作继续使用确定性白名单和真实系统结果，不让 1.7B 对话模型自行拼接或执行系统命令。

## 2. 本段范围

### 2.1 计时器

- 开始指定时长的计时。
- 暂停正在运行的计时。
- 继续已暂停的计时。
- 取消运行中、暂停中或响铃中的计时。
- 查询当前阶段和剩余时间。

### 2.2 Apple Music

- 播放或继续播放。
- 暂停播放。
- 切换上一首和下一首。
- 查询当前歌曲、歌手与播放状态。
- 音乐功能关闭、App 未运行、自动化权限拒绝或脚本失败时返回真实原因。

## 3. 明确不做

- 不扩展日历、待办、剪贴板历史或任意 App 命令栏；这些仍属于当前 PRD 非目标。
- 不允许模型生成 AppleScript、Shell 或任意系统指令。
- 不自动猜测含糊命令；“我喜欢听音乐”“下一首诗写得很好”等普通表达保持聊天。
- 不声称异步播放器状态已经改变；只在 Apple Music 接受脚本指令后反馈已发送，界面仍通过轮询刷新真实状态。

## 4. 技术设计

### 4.1 工具命令

`PetToolCommand` 增加两组白名单命令：

- `timer(PetTimerAction)`：`pause`、`resume`、`cancel`、`status`。
- `music(PetMusicAction)`：`play`、`pause`、`next`、`previous`、`status`。

已有 `startTimer` 和 `open` 命令保持兼容。

### 4.2 路由顺序

1. 识别带合法时长的开始计时命令。
2. 识别必须含“计时”或“倒计时”的生命周期命令。
3. 识别明确的音乐控制词和当前歌曲查询。
4. 识别打开已有功能窗口。
5. 其余内容交给连续对话，不执行工具。

### 4.3 真实执行

- 计时器直接调用现有 `TimerController`，并根据操作前的真实 `CountdownPhase` 返回反馈。
- Apple Music 直接调用现有 `MusicController`；新增明确的 `play` 和 `pause`，避免 `playPause` 在未知状态下反向切换。
- `MusicController.send` 返回真实 `Result`，忙碌、权限拒绝和脚本失败都会进入用户可见反馈。
- 工具结果继续复用开发段 6.4 的 P1 本地语音和派蒙说话动画。

## 5. 验收标准

- “暂停计时”“继续倒计时”“取消计时”“还剩多久”分别命中正确命令。
- 无计时、重复暂停、重复继续和计时已结束时不产生虚假成功反馈。
- “播放音乐”“暂停音乐”“下一首”“上一首”“现在播放的什么歌”命中正确命令。
- Apple Music 未运行时，除播放外的控制不擅自启动播放器。
- 音乐功能关闭时打开设置并说明原因。
- 权限拒绝或命令失败时不返回成功文案。
- 普通聊天不会触发工具。
- 全量 XCTest、构建、安装版计时器真实状态和 Apple Music 实际播放均通过。

## 6. 人工验证边界

计时器状态和路由可由自动化验收。Apple Music 的真实音频播放、暂停和切歌需要本机已有可播放队列，因此最终只在这一项请求人工观察播放器结果。
