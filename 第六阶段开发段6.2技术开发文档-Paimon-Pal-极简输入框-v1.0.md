# 第六阶段开发段 6.2 技术开发文档：Paimon Pal 极简输入框

## 1. 开发目标

将“单击派蒙后弹出屏幕中央大型聊天窗口”改为“在派蒙身边出现轻量输入框”，减少遮挡，让桌面宠物的聊天入口与派蒙本体保持空间关联。

## 2. 本段范围

### 进入本段

- 单击派蒙后显示贴近派蒙的极简输入浮层。
- 初始状态只展示输入框和必要按钮。
- 发送后只展示本次最新回复或生成状态，不展开完整历史记录。
- 继续复用现有本地模型、最近六条上下文和工具调用能力。
- 保留进入完整聊天窗口的次级入口。
- 浮层根据派蒙位置自动选择左侧或右侧，并始终保持在当前屏幕可见区域。
- 输入浮层打开但尚未生成回复时仍可拖动派蒙，浮层随派蒙移动。
- 支持回车发送、发送按钮、关闭按钮和键盘 Esc 关闭。

### 不进入本段

- 不接入正式 TTS 播放。
- 不重做派蒙动画素材。
- 不修改本地模型及其提示词策略。
- 不删除完整聊天窗口及聊天记录能力。

## 3. 界面规格

- 输入态宽度：约 360pt。
- 输入态高度：约 60pt。
- 回复态高度：根据单条回复扩展，但不成为完整聊天窗口。
- 与派蒙间距：12pt。
- 优先显示在派蒙右侧；右侧空间不足时改到左侧。
- 圆角、半透明材质和轻描边，保持桌面悬浮感。
- 回复内容超过限定高度时允许内部滚动或截断，不无限撑大窗口。

## 4. 交互规则

1. 用户单击派蒙，输入浮层出现并自动聚焦。
2. 用户输入文字并按回车或发送按钮。
3. 派蒙进入回复动画，浮层显示“派蒙正在想……”。
4. 工具命令立即展示执行结果；模型对话完成后展示最新回复。
5. 用户可以继续输入下一句，历史上下文仍由现有控制器维护。
6. 用户点击“完整对话”入口后，关闭极简浮层并打开原聊天窗口。
7. 用户点击关闭或按 Esc，浮层关闭并结束当前非生成态交互。
8. 模型正在生成回复时暂时禁止拖动；把派蒙拖回刘海时浮层同步关闭。

## 5. 自动验证要求

- 浮层定位算法覆盖：右侧优先、右侧不足改左侧、上下边界约束。
- 现有对话上下文、计时器和打开功能命令测试不得回归。
- 完整 Xcode 测试全部通过。
- 开发版可以构建并只启动一个 Paimon Pal 实例。

## 6. 人工验收方法

1. 单击桌面上的派蒙，确认输入框出现在派蒙旁边而不是屏幕中央。
2. 输入“你好”并发送，确认出现生成状态和一条最新回复。
3. 再输入“我刚才说什么了？”，确认上下文连续。
4. 点击完整对话入口，确认原聊天记录窗口可以打开。
5. 把派蒙拖到屏幕右边后再次点击，确认输入框自动出现在派蒙左侧且不超出屏幕。
6. 按 Esc 或点击关闭，确认浮层消失而派蒙仍保持在桌面位置。

全部通过后，开发段 6.2 才能创建完成分支。

## 7. 验收中记录的后续问题

- 完整对话记录虽然可以从极简输入框正常打开，并能看到当前会话历史，但仍以传统窗口在屏幕中央弹出，视觉笨重且与派蒙本体割裂。
- 本开发段仅保留该入口，不将其误记为最终界面完成。
- 后续应单独改造成派蒙旁的历史卡片、侧边抽屉或其他贴身面板，并取消默认居中弹窗；具体形态需在进入该开发段前确认。
- 人工验收发现：普通待机状态与打开输入框后的“侧耳聆听”状态视觉尺寸不一致，聆听状态的派蒙明显更大，动作切换时产生割裂感。
- 该问题来自不同动作素材的角色占画比例和基准位置不统一，不属于输入浮层定位逻辑。后续动画一致性开发段应统一角色基准高度、脚底锚点和头顶留白，不能仅依赖窗口整体缩放掩盖。
