# 第七阶段验证报告：Paimon Pal 本地生产力工作台 v1.0

验证时间：2026-09-02 03:31–03:37

验证环境：Apple Silicon MacBook Pro，macOS 26.6.2，Debug 本机临时签名包

结论：自动化验证通过，未发现阻断构建或阻断使用的缺陷。

## 1. XCTest 结果

- 总数：147
- 通过：147
- 失败：0
- 跳过：0
- 最终结果包：`build/ProductivityFinalTestData/Logs/Test/Test-NotchFlow-2026.09.02_03-36-37-+0800.xcresult`

新增覆盖：

- 工作台快照归一化与 JSON 往返。
- 待办排序、随笔智能标题、公开链接安全策略。
- 剪贴板密钥/密码/超长内容过滤。
- 录音索引自动排除丢失的音频文件。
- 保险箱通过内存替身验证，测试不写入用户真实 Keychain。
- 本地 AI 任务请求解析、分段 TCP framing、method/source/大小拒绝。
- 派蒙新建待办/随笔和打开工作台页面的意图路由。
- 原有的刘海布局、动画、Apple Music、文件架、系统状态、计时器、本地对话、语音和连续听说回归全部通过。

## 2. 真实 UI 巡检

| 项目 | 结果 | 证据 |
|---|---|---|
| 工作台窗口 | 通过 | 标准窗口可移动/缩放，黑暗模式布局正常 |
| Bento 首页 | 通过 | 10 个模块正常渲染，底部显示仅本机边界 |
| 8 个导航页 | 通过 | 工作台、待办、随笔记、链接、剪贴板、录音、镜子、保险箱均可进入 |
| 待办闭环 | 通过 | 真实输入“自动验收待办”，添加后正常显示，再删除恢复空状态 |
| 权限静默状态 | 通过 | 未点击录音/镜子时未弹出麦克风或摄像头请求 |

## 3. 本地 AI 任务回调

| 用例 | 期望 | 实际 |
|---|---|---|
| `POST /notify/codex` | 202/OK | 202/OK，首页显示“Codex：自动验收完成” |
| `POST /notify/other` | 拒绝 | 404/ERROR |
| 超过 32 KB 请求体 | 拒绝 | 400/ERROR |
| 监听地址 | 仅回环 | `127.0.0.1:43821` |

## 4. 静态安全检查

- 剪贴板默认关闭且不回读历史。
- 保险箱密文不进入 JSON，从保险箱复制也不会被剪贴板历史采集。
- 镜子离开页面即停止，录音删除移入废纸篓。
- 任务提醒不监听 `0.0.0.0`，不对局域网开放。

## 5. Release 与 DMG

- Release arm64 构建、内置 TTS 运行时预检、本机临时签名和 `codesign --verify --deep --strict` 通过。
- DMG：`dist/Paimon-Pal-0.4.0-10-arm64-local.dmg`，约 4.9 GB。
- SHA-256：`8df39db9e465f6d29ec24ac18fabce0838cf1c2962c00ea4d08de73ccfeed6de`。
- `hdiutil verify` 通过；安装盘内包含 `Paimon Pal.app` 和 `Applications` 快捷方式，App 版本实际读取为 0.4.0 (10)。
- 该包为个人作业用临时签名包，未经 Developer ID 签名和苹果公证，不作为公开发行包。

## 6. 12:00 后人工补验清单

这些项目依赖真实人声、画面或主观质量，不阻断其他开发：

1. 首次点击“开始录音”，允许麦克风和语音识别，说一句普通话后停止；检查 M4A 可播放、时长正常、设备端转写可读。
2. 点击“开启镜子”，允许摄像头；检查预览方向、比例和流畅度，切换到其他页后确认绿色摄像头指示灯消失。
3. 对话中说“帮我新建待办买牛奶”和“记一条随笔……”，检查派蒙反馈、工作台落库和语音播报的主观自然度。
