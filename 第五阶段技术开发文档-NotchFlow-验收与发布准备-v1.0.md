# NotchFlow 第五阶段技术开发文档｜验收与发布准备

> 文档版本：v1.0  
> 对应 PRD：阶段 4｜验收与发布准备  
> 当前分支：`phase/05-acceptance-release-prep`  
> 当前状态：开发段 5.1 已通过，准备进入 5.2 性能与稳定性

---

## 1. 阶段目标

把已经通过分段验收的 NotchFlow，整理成可重复构建、可整体验收、可解释隐私边界并具备本地分发条件的 macOS MVP。

本阶段不使用《AI Agent 产品上线部署手册》的 veFaaS、API 网关、云数据库与对象存储路线。NotchFlow 是纯端侧原生 macOS App，没有 Web 前后端、账号或云数据，因此采用苹果桌面应用的构建、签名、公证与 DMG 流程。

## 2. 开发段划分

### 5.1 全功能总回归

- 将 PRD 14 章验收项映射到自动测试、历史人工证据和本机复测。
- 执行完整 XCTest、Release 构建、沙盒权限与产物结构检查。
- 复核音乐、文件架、系统状态、计时器、设置、全屏和异常恢复的关键闭环。
- 将缺少设备或已经降级的范围明确列为已知边界，不冒充通过。

人工出口：从菜单栏打开设置和四个功能窗口，确认入口完整、App 无崩溃。

### 5.2 性能与稳定性

- 测量空闲与活动状态的 CPU、内存、线程和唤醒情况。
- 检查高频轮询、任务取消、窗口生命周期和长期运行风险。
- 修复明显的性能或资源泄漏问题，并重复测量。

人工出口：连续运行与反复展开、切换模块后，交互仍流畅且无异常升温或卡顿。

### 5.3 隐私、首次启动与版本资料

- 补齐本地隐私说明、权限用途、数据存放与清理边界。
- 增加首次启动引导，说明刘海入口、功能窗口、权限按需申请和文件副本策略。
- 更新 README、版本说明、已知问题和支持矩阵。

人工出口：首次启动说明可读，权限与文件行为没有误导性表述。

### 5.4 本地分发、正式签名与公证

- 生成可重复的 Release App 与 DMG。
- 无 Developer ID 时只能交付本机/测试用临时签名 DMG，并明确 Gatekeeper 限制。
- 有 Apple Developer Program、Developer ID Application 证书和公证凭据后，才执行正式签名、公证与 stapling。
- 正式发布前验证 `codesign`、`spctl`、DMG 挂载、拖入 Applications 和首次启动。

人工出口：在另一台或干净用户环境中安装 DMG，确认启动方式、权限说明和卸载方式均清楚。

## 3. 当前适配结论

- 当前版本：`0.2.0 (1)`。
- 最低系统：macOS 14.0。
- 当前构建架构：Apple Silicon `arm64`；是否补充 Intel `x86_64` 在 5.4 决定。
- App Sandbox：已开启。
- Hardened Runtime：工程配置已开启，正式分发仍需 Developer ID 证书验证。
- 当前签名条件：本机仅有临时签名，`security find-identity` 返回 0 个有效 Developer ID 身份。
- 当前公证条件：未配置 notarytool 凭据。

因此 5.1—5.3 可直接推进；5.4 的本地 DMG 可推进，正式签名与苹果公证必须等待产品经理确认账号条件。

## 4. 验收范围修正

- 音乐首版正式范围以已经确认的 Apple Music 为准；Spotify 已由产品经理明确暂缓，不作为第五阶段阻塞项。
- 亮度没有稳定公开读取接口，继续在设置页明确标记“有限通过”，不显示猜测数值。
- 当前只有一台内置刘海屏 MacBook，macOS 14/15、16 英寸、无刘海外接屏、镜像与合盖模式保留为发布前设备矩阵补验。
- 当前环境无法制造真实磁盘写满，不把磁盘不足的错误分支测试写成真实硬件通过。

## 5. 阶段出口

- 自动测试、Release 构建、产物与权限检查全部通过。
- 本机可执行的 PRD MVP 验收项完成总回归。
- 性能和稳定性达到本地常驻工具的合理水平。
- 隐私、首次启动、版本说明和已知问题齐全。
- 至少生成可验证的本地测试 DMG；正式签名、公证若缺凭据则形成清晰阻塞说明。
- 每个开发段完成后先进行人工验证，再进入下一段。

## 6. 开发段进度

### 6.1 开发段 5.1｜全功能总回归（已通过）

- XCTest：64/64 通过，0 失败，0 跳过。
- Release `arm64` 构建：通过；产物约 2.6 MB，版本 `0.2.0 (1)`，最低系统 macOS 14.0。
- 静态分析：通过。
- 本机临时签名验证：通过；仅用于本机验收，不代表 Developer ID 正式签名。
- Release 浮层：顶部中央 `209 × 39`，启动正常。
- 人工入口回归：设置、音乐、文件架、系统状态和计时器窗口全部可打开。

详细证据见《第五阶段开发段5.1验证报告-NotchFlow-全功能总回归-v1.0.md》。
