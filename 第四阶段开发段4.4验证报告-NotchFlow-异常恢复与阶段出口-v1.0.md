# NotchFlow 第四阶段开发段 4.4 验证报告｜异常恢复与阶段出口

> 文档版本：v1.0  
> 验证日期：2026-08-27  
> 当前结论：通过，允许完成第四阶段并创建完成分支  
> 当前分支：`phase/04-settings-permissions-recovery-complete`

---

## 1. 本段完成内容

- 设置读取时校验布尔值、枚举、显示器标识和数值范围，并将安全值写回 `UserDefaults`。
- 已过期暂停时间在启动时清除；有效暂停状态保留菜单栏恢复入口。
- 计时器持久化数据先做 JSON 解码和字段一致性校验：坏数据清除，合法状态规范化后恢复。
- 文件架索引无法读取或解码时，从 NotchFlow 自有 `Items` 目录恢复暂存副本并原子重建索引。
- Apple Music 权限撤回或播放器退出时移除音乐活动，不影响计时器、文件架和系统状态模块。
- 目标显示器消失时继续沿用 4.3 的主显示器临时回退策略。

## 2. 自动验证

执行命令：

```bash
xcodebuild test -project NotchFlow.xcodeproj -scheme NotchFlow \
  -destination 'platform=macOS' \
  -derivedDataPath build/NotchFlowPhase44Data \
  -resultBundlePath /tmp/NotchFlow-Phase4-4.4-final2-20260827.xcresult
```

结果：

- 总测试数：64
- 通过：64
- 失败：0
- 跳过：0
- `git diff --check`：通过

本段新增覆盖：

- 非法设置值回退后持久写回，避免每次启动重复读取坏值。
- 空白指定显示器标识清除。
- 计时器 JSON 损坏和字段组合不合法时自动清除。
- 新计时器控制器恢复运行中倒计时。
- 文件架损坏索引从实际暂存副本重建。
- 新文件架控制器恢复已落盘副本。
- Apple Music 自动化权限撤回时安全降级。

## 3. 真实 Mac 强退重启冒烟

使用 4.4 Debug App 完成：

1. 启动 4.4 构建，确认进程建立。
2. 对该精确进程执行 `kill -9`，模拟无清理回调的强制退出。
3. 重新启动同一 4.4 App，确认建立新进程。
4. 通过系统窗口列表读取 NotchFlow 顶部浮层。

结果：

- 强退前进程：PID `21150`。
- 重启后进程：PID `21176`。
- 重启后物理刘海浮层：`209 × 39`。
- 重启后坐标：`X=651 / Y=0`。
- 没有残留旧窗口或启动崩溃。

## 4. 人工出口

使用当前正在运行的 4.4 App：

1. 启动一个 5 分钟倒计时。
2. 由 AI 强制结束并重新启动 App。
3. 确认刘海浮层自动恢复，倒计时继续且不是重新从 5 分钟开始。
4. 确认菜单栏入口仍在，已暂存的文件（如有）仍在文件架。

验收标准：重启后应用可见、计时连续、已有本地状态未被异常清空。

外接屏拔插与合盖仍属于 4.3 的设备补验项，不在本段用模拟结果冒充完成。

## 5. 人工验收结果

- 第一次查看时旧计时已经结束，持久化记录按设计清除，因此未将其误判为恢复通过。
- AI 随后在真实 4.4 App 中重新启动 5 分钟计时，并确认落盘状态为 `running`、总时长为 300 秒。
- 对真实 App 执行 `kill -9` 后重新启动，计时记录在启动后持续存在。
- 重启后系统界面读到计时器图标与 `04:28`，证明倒计时沿用原结束时间，没有从 `05:00` 重新开始。
- 产品经理已确认刘海上的绿色计时图标和倒计时可见。

结论：4.4 人工出口通过。
