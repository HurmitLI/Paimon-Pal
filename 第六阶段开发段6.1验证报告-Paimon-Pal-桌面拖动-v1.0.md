# 第六阶段开发段 6.1 验证报告：Paimon Pal 桌面拖动

## 1. 阶段目标

让派蒙不再只能停留在物理刘海下方，同时保留派蒙与刘海之间的归位关系：用户可以把派蒙拖到桌面任意可见位置，也可以把它拖回刘海收起。

本开发段不改造对话窗口，也不接入正式 TTS 播放链路。

## 2. 已实现能力

- 派蒙在待机状态下支持鼠标拖动。
- 使用 6pt 拖动阈值区分单击和拖动，避免轻微移动误触发。
- 拖动后派蒙保持在桌面，不因鼠标移开而自动消失。
- 单击派蒙仍可正常打开陪伴对话页面。
- 拖回物理刘海附近时自动吸附、播放钻回动画并恢复默认隐藏状态。
- 桌面位置按显示器和归一化坐标保存，应用重启后恢复。
- 派蒙窗口被限制在当前显示器可见区域内，不会被拖出屏幕。
- 聆听等非待机状态不允许开始拖动，避免手势与交互状态冲突。

## 3. 自动验证

- Xcode 自动测试：89/89 通过，0 失败，0 跳过。
- 覆盖桌面位置保存、恢复、清除和异常数据降级。
- 覆盖坐标换算、重启恢复和屏幕边界约束。
- 覆盖仅待机状态允许拖动的交互规则。
- 开发启动脚本完成 zsh 语法检查与真实构建启动验证。

## 4. 人工验收记录

| 验收项 | 人工结果 |
| --- | --- |
| 刘海悬停只出现一个派蒙，动画稳定 | 通过 |
| 按住派蒙可拖到桌面其他位置 | 通过 |
| 拖动后单击仍可打开对话页面 | 通过 |
| 远离刘海松手后保持在桌面 | 通过 |
| 拖回刘海后自动吸附并钻回 | 通过 |
| 重启应用后恢复上次桌面位置 | 通过 |
| 拖到屏幕边缘时保持完整可见 | 通过 |

## 5. 验收中发现并修复的问题

人工验收时曾出现多个派蒙同时显示并持续抖动。核查后确认不是单个窗口动画重影，而是 `/Applications` 中的安装版与构建目录中的开发版同时运行。

处理结果：

- 结束重复实例，只保留当前开发版验证。
- 清理本次测试遗留位置后重新验证。
- 开发启动脚本增加旧实例关闭与退出确认，防止以后再次同时运行两个 Paimon Pal/NotchFlow 进程。

## 6. 阶段结论

开发段 6.1 验收通过。派蒙已经具备“从刘海出场—拖到桌面—保持与记忆位置—拖回刘海归位”的完整桌面宠物链路。

下一开发段建议进入派蒙身边的极简对话输入框：用贴近派蒙的小型输入浮层替代默认占据屏幕中央的大型聊天窗口，同时保留进入完整对话页的入口。
