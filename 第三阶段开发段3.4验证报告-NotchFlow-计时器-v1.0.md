# NotchFlow 第三阶段开发段 3.4 验证报告｜计时器

> 验证日期：2026-08-27
> 当前分支：`phase/03-core-modules`
> 当前结论：计时器开发段通过，等待第三阶段总回归

## 1. 本段实现

- 提供 5、10、15、25、30、60 分钟快捷时长，以及自定义分钟和秒数。
- 单计时器支持启动、暂停、继续、手动结束和完成确认。
- 使用目标结束时间计算剩余时间，不依赖每秒累加，降低睡眠和调度延迟造成的漂移。
- 监听系统唤醒和系统时钟变化，恢复后重新校准剩余时间。
- 使用本地 `UserDefaults` 保存运行状态；App 重启后恢复运行中、暂停或最近完成的计时器。
- 紧凑刘海显示 `MM:SS`；最后 60 秒使用橙色强调，不闪烁。
- 展开刘海左侧提供暂停、继续或完成确认，右侧显示剩余时间与完整窗口入口。
- 完成后刘海显示“计时结束”，用户可确认收起；未操作时最多保留 30 秒后自动收起。
- 系统通知为可选能力，只有用户主动点击“启用通知”才申请权限；拒绝或未启用不影响刘海提醒。

## 2. 自动化验证

执行结果：XCTest 共 39 项，39 项通过，0 失败，0 跳过。

本段新增覆盖：

- 目标结束时间可正确跨过普通调度间隔和睡眠时间差。
- 暂停后剩余时间冻结，继续后按新目标结束时间运行。
- App 重启时可恢复未来未完成、暂停和最近完成的计时状态。
- 过期过久的完成提醒不会在重启后反复出现。
- 时间文本格式化与本地状态存取正确。
- 计时活动正确进入状态协调器；取消或确认完成后移除活动并收起刘海。

构建结果：Debug App 构建成功，本地签名成功，`git diff --check` 通过。

## 3. 真实界面自审

| 验证项 | 结果 | 证据 |
|---|---|---|
| 普通计时器窗口 | 通过 | 快捷时长、自定义时长、通知说明和控制按钮均正常显示 |
| 启动与紧凑态 | 通过 | 30 秒计时启动后，紧凑刘海显示计时器图标和剩余时间 |
| 暂停 | 通过 | 在 00:25 暂停并等待后仍保持 00:25 |
| 继续 | 通过 | 继续后剩余时间恢复递减 |
| 展开态控制 | 通过 | 左侧暂停/继续，右侧显示剩余时间和完整窗口入口 |
| 重启恢复 | 通过 | 暂停于 00:15 后退出并重启 App，仍恢复为暂停 00:15 |
| 完成提醒 | 通过 | 3 秒真实倒计时结束后，紧凑刘海显示“计时结束” |
| 完成确认 | 通过 | 展开后点击确认，刘海立即收起并恢复纯黑刘海 |
| 遮挡 | 通过 | 沿用统一顶部高度，只横向展开，不向下覆盖应用文字 |

## 4. 产品经理人工验收

产品经理已在真实 App 中逐项确认：

1. 打开计时器普通窗口正常。
2. 点击 5 分钟可启动真实计时。
3. 关闭普通窗口后，刘海紧凑态剩余时间持续递减。
4. 点击刘海后，左侧控制按钮、右侧剩余时间和窗口入口显示正常。
5. 暂停后剩余时间冻结；继续后恢复递减。
6. 可从刘海重新打开完整计时器窗口。
7. 点击“结束计时”后恢复到快捷时长初始界面。

继续动作曾因刚点击时数字尚未跨过下一秒而被误判为未读秒。AI 随后在同一真实 App 中暂停于 `02:26`，等待确认保持不变；点击继续后等待约 2 秒变为 `02:24`。产品经理再次观察后确认正常，因此未将其记录为代码缺陷。

## 5. 已修复问题

第一次完成态复测发现：点击完成确认后，计时活动虽然已经变为空闲，但展开刘海仍可能残留 `00:00` 空壳。

修复方式：计时器进入空闲态时同时移除 `timer.active` 活动；如果当前刘海仍处于计时器展开态，则立即执行收起。随后增加自动回归测试，并再次运行 3 秒真实倒计时，确认问题不再出现。

## 6. 阶段出口待办

计时器开发段本身已完成。创建第三阶段完成分支前，还需按“一次一个操作”完成：

1. 回归文件架从其他 App 拖入和拖出的真实鼠标手势。
2. 运行第三阶段四模块总回归并记录分支。
