# NotchFlow 第五阶段开发段 5.2 验证报告｜性能与稳定性

> 文档版本：v1.0  
> 验证日期：2026-08-27  
> 当前结论：通过，允许进入开发段 5.3  
> 当前分支：`phase/05-acceptance-release-prep`

---

## 1. 性能问题与修复

首次在关闭普通功能窗口的 Release App 上连续采样 21 秒：

- 静默 CPU 平均：1.57%
- 静默内存：28.5 MB

内存通过 PRD 的 100 MB 目标，但 CPU 未达到低于 1% 的目标。调用采样确认主要消耗来自 Apple Music 已停止时仍每秒重新编译并执行 AppleScript。

本段完成以下优化：

- Apple Music 元数据 AppleScript 只编译一次并复用。
- 播放中保持每秒读取；暂停、停止和不可用状态改为每 2 秒读取。
- 电池从每秒读取改为每 30 秒读取；音量维持 250 毫秒刷新。
- 空闲或暂停计时器不再周期性保存相同状态；运行和响铃仍按 250 毫秒刷新。

## 2. 优化后测量

在相同 Release、相同 Mac、关闭普通功能窗口的条件下连续采样 21 秒：

- 静默 CPU 平均：0.66%
- 静默内存平均：29.6 MB
- 平均线程数：8.3

结果：CPU 低于 1%，内存低于 100 MB，达到 PRD 12.1 的静默目标。

## 3. 启动与压力检查

三次从退出到刘海浮层可见的本机测量：

- 第一次：461 毫秒
- 第二次：456 毫秒
- 第三次：453 毫秒

三次均低于 PRD 的 2 秒启动目标。该结果是当前 Mac 的本地测量，不冒充其他机型或系统版本数据。

自动把鼠标在刘海内外往返 20 次后：

- 进程未崩溃或重启。
- 浮层仍位于顶部中央，尺寸 `209 × 39`。
- RSS 只波动约 0.4 MB，没有观察到持续增长。
- 功能窗口由一个 `UtilityWindowController` 复用，不会每次打开都创建永久窗口。

## 4. 自动验证

- XCTest：65/65 通过，0 失败，0 跳过。
- 新增音乐播放状态轮询频率测试。
- Release `arm64` 构建：通过。
- Debug 静态分析：通过。

Release 静态分析命令会同时分析测试目标，但 Release 产品模块没有开启 `ENABLE_TESTABILITY`，因此 `@testable import NotchFlow` 无法加载。该问题属于分析命令与测试目标的构建配置不匹配；Debug 分析已正常完成，Release 产品构建本身通过。

## 5. 人工出口

产品经理在优化后的 Release App 中连续执行 5 次“鼠标移入刘海并等待展开，再移开收起”，确认交互流畅，没有抖动或卡顿。

结论：开发段 5.2 通过，可以进入 5.3“隐私、首次启动与版本资料”。
