# 第六阶段开发段 6.4 技术开发文档：Paimon Pal 本地语音正式接入

## 1. 开发目标

把听感复选中通过的 P1“清脆反应感版”原创幻想伙伴声线正式接入 Paimon Pal，使本地模型回复和工具执行结果可以在显示文字的同时离线播报，并与派蒙说话动画同步。

## 2. 已确认输入

- 对话模型：App 已内置 Qwen3 1.7B 4-bit 本地模型。
- TTS 模型：`Qwen3-TTS-12Hz-1.7B-VoiceDesign-4bit`，本机模型约 2.31GB。
- 声线：P1“清脆反应感版”。
- 固定声线描述：原创年轻奇幻伙伴女声，明亮高扬、轻巧有弹性、语速较快、元气好奇、重音活泼、句尾自然上扬，不模仿真人或现有角色。
- 固定随机种子：`20260931`。
- 固定参考声纹：用户已通过的 `P1-清脆反应感版.wav`，连同准确文字稿一起内置到 App。
- 运行环境：Apple Silicon、独立 Python 3.12 与 MLX Audio 0.5.0。

## 3. 本段范围

### 3.1 语音生成

- 文字回复产生后，按设置决定是否启动本地 TTS。
- TTS 作为独立子进程按需启动，生成完成后退出并释放模型内存。
- 每次只允许一个生成任务；新回复到达时取消旧生成和旧播放。
- 输出文件写入 App 沙盒的 Application Support 临时语音目录，不写入项目目录。
- 限制单次播报文本长度，去除 Markdown 符号和不适合朗读的内容。
- TTS 只朗读回复原文，不再为每条回复强制添加“旅行者”。如果原回复本身自然以“旅行者”开头，界面文字保留原标点；仅在 TTS 输入中移除称呼后紧邻标点，避免拖音、唱腔或逐字强调。

### 3.2 播放与动画

- 生成期间文字先显示，不阻塞聊天和工具执行。
- 音频真正开始播放时进入派蒙“说话”动画。
- 播放自然结束、用户停止、语音关闭或新请求打断时退出说话动画。
- 不复用“回复成功”动画冒充正在播放；成功动画与真实语音状态分离。

### 3.3 设置与降级

- 设置页新增“派蒙语音”开关，默认开启。
- 缺少模型、运行时异常、生成失败或播放失败时保持文字结果，不影响计时和窗口工具。
- 失败只显示必要的非阻断提示，不循环重试、不自动联网下载。

### 3.4 工具播报

- 开始计时和打开设置/音乐/文件架/系统状态/计时器的真实执行结果进入同一语音链路。
- 语音只复述已经完成的工具结果，不让 TTS 参与工具决策。

### 3.5 自包含安装版

- 源码仓库不提交私有 TTS 权重、候选样音或 Python 虚拟环境。
- 本机构建脚本把 Python 3.12 运行时、所需 site-packages、P1 固定参考声纹、生成脚本和 TTS 模型装入 App Resources。
- 运行时只读取 App 内资源；不依赖用户预先安装 Python，不从 Hugging Face 联网下载。
- 不再为每句文本单独执行 VoiceDesign。运行时使用固定 P1 样音的 ICL 参考声纹，让不同内容共用同一基础音色，仅保留合理的语气与节奏变化。

## 4. 不进入本段

- 不做语音输入、麦克风录音或 ASR。
- 不做真人或现有角色声纹克隆。
- 不重做完整聊天记录窗口。
- 不增加新的 Agent 工具。
- 不做 Developer ID 签名、公证或公开分发。

## 5. 状态与并发规则

1. 新文字结果到达。
2. 取消正在生成或播放的旧语音。
3. 若语音开关关闭，停在文字结果。
4. 若开启，清理文本并启动独立 TTS 进程。
5. 生成完成且请求仍为最新时开始播放，并进入说话动画。
6. 播放结束后停止说话动画并清理临时 WAV。
7. 任一步失败都回到文字可用状态。

## 6. 自动验收

- 文本清理、长度限制、不强制添加称呼和原文自带称呼时的拖音抑制测试通过。
- 对“你是不是每次都加旅行者”类的行为反馈做直接意图识别，先承认问题并说明调整，不交给小模型自由联想。
- 新请求可使旧请求失效，过期结果不会播放。
- 关闭语音后不启动生成；关闭时正在播放的音频会停止。
- 缺少运行时、生成失败、播放失败均降级为文字。
- AppPreferences 语音开关持久化测试通过。
- 完整 XCTest、Debug 构建、签名、安装版资源检查和单实例运行通过。
- 自包含 Python 可在脱离项目虚拟环境的路径下导入 MLX Audio，并使用 App 内模型生成 WAV。
- 同一文本由两个独立进程生成的 WAV 字节完全一致；安装版的不同文本均显式使用同一 `VoiceReference.wav`。

## 7. 最终人工验收

自动验收完成后，用户只需完成一次主观检查：

1. 输入“我今天有点累”，确认文字先出现，随后以 P1 声线朗读，派蒙在真实播放期间进入说话动画。
2. 输入“帮我计时一分钟”，确认工具已执行、语音播报且刘海倒计时递减。
3. 在播放中再次发送一句话，确认旧语音立即停止，后续语音不重叠。

三项通过后创建 6.4 完成分支，再进入工具能力扩展阶段。
