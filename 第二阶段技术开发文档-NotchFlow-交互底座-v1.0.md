# NotchFlow 第二阶段技术开发文档｜交互底座

> 文档版本：v1.1
> 更新日期：2026-08-26
> 对应 PRD：阶段 1｜交互底座
> 前置留档：Git 分支 `phase/01-feasibility-complete`，提交 `57fa1fa`

## 1. 阶段目标

把第一阶段证明可行的屏幕几何与 `NSPanel` 配置，整理成正式产品可复用的交互底座。

本阶段只完成：

1. 正式 NotchFlow 原生 App 骨架。
2. 屏幕几何与顶部锚定布局服务。
3. S0–S5 刘海展示状态与 S6 普通功能窗口。
4. 活动优先级与临时活动恢复规则。
5. 收起、悬停、两翼快捷控制、文件拖入反馈和普通功能窗口闭环。
6. 标准动画与 macOS“减少动态效果”适配。

## 2. 明确不做

- 不开发正式音乐模块。
- 不开发正式文件暂存与 AirDrop 业务。
- 不开发电源、音量等正式系统 HUD。
- 不开发计时器。
- 不开发设置页、权限引导、持久化和发布包。
- 不把第一阶段实验 Dashboard 视觉带入正式 App。

## 3. 分段与人工验收

### 开发段 2.1：正式骨架与核心契约

- 建立 `NotchFlow.xcodeproj`、正式 App target 和 XCTest target。
- 建立 `ScreenGeometryService`、`ActivityCoordinator`、状态与优先级契约。

自动验收：状态优先级、恢复规则、布局尺寸测试全部通过。

### 开发段 2.2：窗口与交互闭环

- 建立正式 `NSPanel`。
- 实现静默收起、180ms 悬停预览、点击展开、点击外部收起。
- 物理刘海的空闲悬停态仅保留与静默态一致的 7pt 底部点击热区，内容放在刘海两翼。
- S4 取消 380 × 148pt 中央大浮层，改为刘海两翼紧凑快捷控制。
- S1–S5 在物理刘海屏上统一保持 `notchHeight + 7pt` 高度，只横向变化，不覆盖应用顶部内容。
- 实现文件进入热区的最高优先级反馈。
- S6 改为普通、可移动、不置顶的 macOS 功能窗口。

人工验收：按 AI 单步引导操作，不要求产品经理理解窗口 API。

### 开发段 2.3：动画与无障碍

- 标准模式使用约 0.4 秒的弹性尺寸过渡。
- 减少动态效果时取消弹性过冲，改为 150–200ms 平滑过渡。
- 状态内容与尺寸同步变化。

人工验收：分别观察标准动画和系统“减少动态效果”模式。

### 开发段 2.4：回归与阶段出口

- 运行 XCTest、签名校验、真实启动和窗口坐标检查。
- 输出第二阶段验证报告。
- 第一阶段实验工程保留为证据，不作为正式 App 代码入口。

## 4. 状态与优先级

| 状态 | 本阶段实现 |
|---|---|
| S0 静默收起 | 是 |
| S1 紧凑活动 | 是，使用模拟活动验证容器，不接业务 |
| S2 临时 HUD | 是，使用模拟事件验证排队和恢复 |
| S3 悬停预览 | 是 |
| S4 两翼快捷控制 | 是，不向下展开大浮层 |
| S5 文件接收 | 是，只验证拖入反馈，不复制文件 |
| S6 普通功能窗口 | 是，只验证窗口形态，不开发正式功能 |

优先级从高到低：文件拖入、重要系统状态、用户展开、计时器关键态、计时器、音乐、普通 HUD、静默。

## 5. 阶段出口条件

- 正式 App 与实验 App 完全分离。
- 状态机与具体业务模块无直接依赖。
- 普通 HUD 不覆盖用户正在操作的两翼快捷控制，而是排队等待。
- 文件拖入反馈可以抢占当前视觉状态，拖离后恢复。
- 物理刘海顶部锚点在全部尺寸变化中保持不动。
- 点击刘海不抢走当前应用键盘焦点。
- 普通桌面与全屏 Space 均可显示。
- 标准动画和减少动态效果均通过人工验收。
- 自动化测试全部通过后，才交付第二阶段人工验收。
