# 第六阶段开发段 6.9 技术开发文档：Paimon Pal 真实中间帧动画升级

## 1. 本段目标

本段解决“8 帧动画偏硬、待机动作单调”的素材问题，不通过简单提高播放速度或叠加透明帧冒充流畅度。

升级范围：

1. 待机循环：16 个独立真实帧，12 fps。
2. 点击反馈：16 个独立真实帧，16 fps。
3. 聆听循环：16 个独立真实帧，12 fps。
4. 说话循环：16 个独立真实帧，16 fps。

## 2. 为什么不直接做 60 帧

60 fps 是每秒的显示次数，不等于必须为每一秒手工绘制 60 张。对小尺寸桌宠，真实观感主要取决于：

- 相邻帧的姿势差足够小。
- 角色中心和脚底锚点稳定。
- 循环首尾能自然衔接。
- 不存在白底、黑底、双影或突然缩放。

本段选择 12–16 fps 的 16 帧动作表，在视觉连续性和本地内存占用之间保持平衡。

## 3. 素材规格

- 动作表：4 列 × 4 行，从左到右、从上到下播放。
- 输出尺寸：1256×1256 px，单帧 314×314 px。
- 背景：真实 Alpha 透明，禁止可见网格、黑色刘海、白边和拖影。
- 锚点：脚底 Y=302 px，角色中心 X 波动不超过 1 px。
- 运行时：仍由 `NotchPetFrameNormalizer` 统一到 512×512 画布，不改变桌宠外层面板尺寸。

## 4. 素材生产与检查链路

1. 图像生成输出纯品红背景的 4×4 动作表。
2. `prepare_pet_animation_sheet.py` 以确定性方式去除色度背景，抽取 16 个完整角色连通区。
3. 每帧放入固定大小透明格子，仅作位置对齐，不生成或插值像素。
4. `audit_pet_animation_motion.py` 检查重复帧、剪裁、轮廓面积突变、中心漂移、脚底漂移和边缘残留。
5. `render_pet_animation_preview.py` 生成 GIF 供最终人眼检查。

## 5. 接入策略

- `NotchPetMotion` 根据动作独立指定 2 行或 4 行动作表。
- 动画实验室的清单格式支持每个动作单独声明网格，兼容原有 8 帧转场。
- 新素材使用新版本文件名，旧素材不覆盖，可快速回退。
- `emerge` 和 `return` 保留用户已验收的 8 帧转场，本段不重做路径和裁切逻辑。

## 6. 不在本段范围

- 不改变刘海面板尺寸、桌面拖动逻辑或对话 UI。
- 不修改 4B 本地模型、TTS 或连续听说。
- 不以运行时交叉淡化补帧，避免重影。
- 不把私有课程演示素材提交到 Git 仓库。
