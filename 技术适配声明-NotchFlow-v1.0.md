# NotchFlow 技术适配声明

> 文档版本：v1.0  
> 更新日期：2026-08-26  
> 输入文档：《NotchFlow（暂定名）产品需求文档 v1.0》《AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1》  
> 当前状态：只完成技术适配与开发文档，不创建工程、不安装依赖、不编写代码

---

## 0. 结论摘要

NotchFlow 是一个**纯端侧、强系统能力依赖的原生 macOS 工具**，不是 AI Agent、Web 产品或 API 服务。通用手册中的安全、可恢复、可验证和错误边界继续强制执行；Python、FastAPI、Pydantic、Next.js、数据库、模型调用、SSE 和云部署等默认方案不适用于当前项目。

推荐采用：

- Swift + SwiftUI + AppKit 原生实现。
- 模块化单体，不拆微服务，不建立后端。
- 终端能力先行的纵向技术验证。
- XCTest 自动化测试 + 真实 MacBook 人工/集成冒烟。
- 首阶段零第三方依赖，先用苹果系统框架证明关键能力可行。
- 首阶段只验证，不开发正式 UI 和四个完整业务模块。

本声明没有新增收费平台、云服务、账号体系、数据上传或 AI 模型费用。

---

## 1. 产品形态判断

### 1.1 产品类型与核心任务

- 产品类型：原生 macOS 菜单栏工具和顶部悬浮交互面板。
- 目标终端：带物理刘海的 MacBook；同时兼容无刘海外接显示器。
- 核心任务：在刘海附近完成音乐控制、文件暂存/AirDrop、系统状态反馈和计时器操作。
- 数据形态：本地设置、计时器状态、文件暂存副本和文件索引。
- 网络依赖：MVP 核心功能不要求联网。
- AI/Agent：无，不调用模型，不需要 Prompt、向量库或 Agent 编排。

### 1.2 核心交互

核心交互属于“持续系统事件 + 状态机 + 原生桌面交互”：

- 静默收起、紧凑活动、临时 HUD、悬停、展开、文件接收和二级面板。
- 鼠标、触控板、拖放、系统电源/音量变化和播放器状态共同驱动。
- 多个活动按照确定性优先级处理，不允许模型动态决策。
- 交互本身决定产品是否可用，不是后端能力的展示外壳。

### 1.3 开发路径

选择：**终端能力先行的纵向技术验证**。

不采用“后端先行”，原因如下：

1. 项目没有后端和远程业务能力。
2. 最大风险来自 macOS 窗口、Space、屏幕刘海、Apple Events、Core Audio、文件沙盒和 AirDrop。
3. 这些能力必须在真实 Mac 和最小原生界面中验证，命令行 mock 不能证明交互可用。
4. 在系统能力没有证明之前制作正式视觉和完整状态机，会产生较大返工。

### 1.4 当前阶段范围

第一工程阶段定义为：**技术可行性验证，对应 PRD 的“阶段 0”**。

验证五类问题：

1. 物理刘海尺寸与外接屏降级检测。
2. `NSPanel` 在菜单栏、Space、全屏和多显示器中的行为。
3. Apple Music 与 Spotify 的信息读取和控制边界。
4. 电源、电量、音量和亮度的监听边界。
5. 文件拖入、沙盒复制、拖出和 AirDrop 的完整可行性。

当前用户要求只产出文档，因此以上验证尚未执行。

---

## 2. 采用的默认方案

这里的“默认方案”指继续采用通用手册中适用于本项目的工程规则，不代表套用其 Web/Agent 技术栈。

| 方案 | 采用原因 |
|---|---|
| Git 版本管理 | 保存可恢复变更、便于审查和阶段回退；属于强制工程底线 |
| 模块化单体 | 单用户端侧应用不需要微服务，模块边界足以隔离系统能力风险 |
| 明确状态枚举 | 刘海状态和能力验证结果都需要确定性流转，不能依赖散落布尔值 |
| 本地持久化 | 产品明确本地优先，不增加服务器和云端数据库 |
| 结构化错误 | UI 只显示可理解的错误，不向用户展示调用栈或系统隐私数据 |
| 自动化 + 真实设备双层验证 | mock 能验证计算和状态，真实 Mac 才能验证窗口、权限、拖放和播放器 |
| 依赖最小化 | 第一阶段先使用系统框架，避免第三方库掩盖系统能力边界 |
| 渐进式权限 | 首次使用相关功能时再申请，拒绝后只降级对应模块 |

---

## 3. 触发的按需模块

| 模块/能力 | 触发需求 | 第一阶段是否引入 | 说明 |
|---|---|---|---|
| SwiftUI | 最小原生验证界面 | 是 | 只做实验入口，不做正式视觉 |
| AppKit / `NSPanel` | 刘海悬浮窗口、窗口层级、鼠标事件 | 是 | 产品核心系统能力 |
| `NSScreen` 安全区域 API | 物理刘海和多屏几何 | 是 | 禁止机型白名单硬编码 |
| Core Audio | 音量和静音变化监听 | 是 | 只验证读取/监听，不替换系统 HUD |
| IOKit Power Sources | 电源、电池与充电状态 | 是 | 只读取，不修改系统电源策略 |
| Apple Events / ScriptingBridge | Apple Music、Spotify 控制 | 是 | 按目标应用分别验证授权、拒绝和撤销 |
| Uniform Type Identifiers | 拖入对象类型检查 | 是 | 不执行拖入内容 |
| App Sandbox | 文件访问与未来商店兼容性 | 是 | 第一阶段在沙盒开启状态下验证 |
| `NSSharingService` | AirDrop | 是 | 只验证唤起系统分享流程，不自动发送 |
| `OSLog` | 测试证据和错误分类 | 是 | 不记录文件内容、账号信息和敏感路径 |
| UserNotifications | 计时结束提醒 | 否 | 计时器不在第一阶段范围 |
| `SMAppService` | 开机启动 | 否 | 设置与权限属于后续阶段 |
| SQLite/ORM | 多实体、复杂查询和并发 | 否 | 当前没有触发条件 |
| 网络服务/API | 跨终端或云端能力 | 否 | PRD 明确本地优先 |
| AI 模型/RAG/SSE | 模型生成或流式输出 | 否 | 产品没有 AI 能力 |

---

## 4. 偏离或暂缓的默认方案

| 手册默认方案 | 本项目替代方案 | 原因 | 影响 | 重新评估条件 |
|---|---|---|---|---|
| Python 3.11 + FastAPI | Swift 原生 macOS 应用 | 核心能力全部位于 Mac 系统层 | 不提供 HTTP API | 未来出现跨设备或远程业务能力 |
| Pydantic | Swift 强类型、枚举和显式校验器 | 没有 Python 接口 | 校验集中在领域模型和文件导入边界 | 引入后端时 |
| pytest | XCTest | 与 Xcode/macOS 原生工程匹配 | 测试命令改为 `xcodebuild test` | 无需恢复 pytest |
| Next.js + TypeScript | SwiftUI + AppKit | PRD 明确原生桌面端，不采用网页套壳 | 不能复用 Web UI，但获得完整 macOS 能力 | 未来独立开发 Web 管理端 |
| 后端先行 | 终端能力先行的纵向验证 | UI、窗口和系统权限决定可行性 | 第一阶段会出现最小原生实验界面 | 系统能力全部验证后进入常规模块开发 |
| `/api/v1` API | 内部 Swift Protocol 与 Service 接口 | 无进程外调用方 | 不生成 Swagger 或网络接口 | 出现外部客户端时 |
| `.env` 配置 | 无秘密配置；普通设置使用 typed defaults | 无 Key、数据库密码或远程服务 | 不创建空 `.env` | 引入需保密的远程能力时 |
| SQLite + SQLAlchemy + Alembic | 第一阶段只产出版本化能力报告；产品阶段使用本地文件索引/设置存储 | 当前无复杂关系或并发写入 | 不提前建立数据库迁移负担 | 多文件关系、复杂筛选或并发修改出现时 |
| SSE/任务队列 | 不引入 | 没有长耗时远程任务 | 无流式协议 | 出现远程长任务时 |
| Docker/veFaaS | 后续签名、公证和 DMG | 本产品直接运行在 macOS | 不提供网页访问地址 | 若增加后端服务再评估 |

### 4.1 暂缓的产品能力

第一阶段只验证技术，不实现：

- 正式灵动岛状态机和优先级队列。
- 苹果规格的完整视觉、动画和减少动态效果。
- 完整音乐、文件架、系统 HUD 和计时器模块。
- 设置、权限中心、开机启动和数据恢复 UI。
- 签名、公证、DMG 和 Mac App Store 上架。

这些暂缓项不改变 PRD，只防止技术验证阶段范围蔓延。

---

## 5. 强制底线检查

### 5.1 密钥与隐私：通过

- 当前阶段没有 API Key、账号密码或远程服务。
- 不读取或记录音乐账号凭据。
- 不记录拖入文件内容、完整文件名或用户目录路径。
- AirDrop 必须由用户在系统界面中确认，不自动选择设备或发送。
- 后续若出现秘密配置，优先使用 Keychain 或不会提交仓库的秘密载体。

### 5.2 数据与任务可恢复：计划通过

- 第一阶段不保存正式用户业务数据。
- 能力验证结果使用带 schema 版本的 JSON 报告。
- 报告采用临时文件写入后原子替换，防止中途损坏。
- 正式计时器和文件索引的恢复能力留到对应开发阶段实现。

### 5.3 输入输出校验：计划通过

- 屏幕几何：校验宽高为有限正数、区域不越出屏幕、刘海检测失败时进入悬浮胶囊降级。
- 系统数值：音量、亮度和电量统一标准化到 `0...1`，越界值拒绝展示。
- 文件拖入：校验 URL、存在性、类型、大小、目标空间和安全文件名。
- Apple Events：只允许 Apple Music 和 Spotify 两个明确目标，不向任意应用发送事件。
- 能力报告：使用受控枚举，不写入未定义状态。

### 5.4 错误与日志边界：计划通过

- 用户可见错误只包含错误码、简明说明和建议动作。
- 调试日志可包含系统版本、能力名、结果和耗时，不包含账号、文件内容、完整路径或调用栈到产品 UI。
- 权限拒绝、目标应用未安装、API 不可用和系统版本不支持必须是正常分支，不作为崩溃处理。
- 单个实验失败不得中止其他能力验证。

### 5.5 测试与真实验收计划

本项目不涉及模型调用，因此不执行“mock + 真实模型”测试。等价替换为：

1. 第一层：XCTest 自动化测试，覆盖几何计算、状态枚举、值校验、文件命名和能力报告。
2. 第二层：真实 MacBook 集成/人工冒烟，覆盖物理刘海、Space、全屏、多屏、Apple Music、Spotify、系统音量/电源、文件拖放和 AirDrop 系统面板。

自动化结果不能冒充真实系统能力验证；真实验证缺少设备或目标应用时必须标记“待验”或“阻塞”。

---

## 6. 当前环境与现有项目自检

### 6.1 当前环境

| 项目 | 检查结果 | 对后续开发的影响 |
|---|---|---|
| macOS | 26.6.2（Build 25G83） | 可作为当前系统真实验证环境，但仍需覆盖 PRD 的最低系统版本 |
| 设备 | MacBook Pro，Mac17,2，Apple M5，24GB | 具备内置 Liquid Retina XDR 屏，可用于物理刘海验证 |
| 架构 | arm64 | 第一阶段先验证 Apple Silicon |
| 内置屏幕 | 3024 × 1964 Retina | 当前可验证内置屏；无刘海外接屏需另备设备 |
| Swift CLI | Apple Swift 6.3.3 | 能检查基础 Swift，但不能替代完整 Xcode |
| Git | 2.50.1 | 可用 |
| Apple Music | 已安装 | 可执行真实权限和控制验证 |
| Spotify | 已安装 1.2.97.270 | 真实能力验证等待用户登录并播放测试曲目 |
| 完整 Xcode | 未安装或未被发现 | **开发阻塞项**；当前只有 Command Line Tools |
| Git 仓库 | 当前目录不是仓库 | 文档阶段无影响；正式开发前初始化 |
| 现有源码 | 无 `.swift`、`.xcodeproj`、entitlements 或 Info.plist | 这是从零开始的原生项目 |

### 6.2 开工前必须补齐

1. 安装完整稳定版 Xcode，并确认 `xcodebuild -version` 正常返回。
2. 首次启动 Xcode，完成组件安装和许可确认。
3. 根据是否完成 Spotify 首版验收，安装 Spotify 桌面客户端。
4. 准备一个无刘海外接显示器用于降级模式验证。
5. 正式开发开始时初始化 Git，并先提交文档基线。

这些动作不在本次“只产出文档”的授权范围内，本次没有执行。

---

## 7. 需要产品经理决定的问题

无。

以下事项不阻塞当前文档确认：

- 正式产品名和正式 Bundle ID 在发布准备前确认。
- Apple Developer Program 付费账号在签名、公证阶段再确认；第一阶段本地验证不新增该费用。
- 2026-08-26 阶段决策：产品经理确认第一阶段以 Apple Music 验证结果为准，跳过 Spotify 实测。该决定只调整第一阶段出口，不自动删除 PRD 中的 Spotify；正式功能开发前再确认其 MVP 优先级。

---

## 8. 产品经理确认清单

只需确认以下三点：

- [ ] 对产品理解正确：这是本地原生 Mac 刘海工具，首版四个核心功能不变。
- [ ] 没有新增 PRD 未要求的收费平台、云服务、数据上传或 AI 模型。
- [ ] 第一阶段只做技术可行性验证，不做正式产品功能和视觉，不影响后续按 PRD 继续开发。

---

## 9. 配套文档

- [NotchFlow PRD v1.0](./PRD-MacBook-Notch-Island-v1.0.md)
- [AI Agent 产品 Vibe Coding 通用技术栈手册 V2.1](./AI产品Vibe%20Coding通用技术栈手册(1).md)
- [第一阶段技术开发文档｜技术可行性验证](./第一阶段技术开发文档-NotchFlow-技术可行性验证-v1.0.md)
