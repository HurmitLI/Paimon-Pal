# NotchFlow 第三阶段开发段 3.3 验证报告｜系统状态

> 验证日期：2026-08-27
> 当前分支：`phase/03-core-modules`
> 验证结论：通过

## 1. 本段实现

- 使用 `IOKit Power Sources` 读取真实电量、供电来源和充电状态。
- 使用 `Core Audio` 读取默认输出设备音量和静音状态。
- 电源接入、断开、低电量、音量变化、静音与解除静音映射为刘海 S2 HUD。
- 普通 HUD 展示 1.5 秒，低电量展示 3 秒。
- 连续音量变化始终使用 `system.audio`，原地更新内容与退出时间，不重复创建活动。
- HUD 左侧显示系统图标，右侧显示数值与 0–100% 进度。
- 增加“系统”普通功能窗口，展示当前电池、电源、音量和静音真实读数。
- 亮度明确显示“当前版本不可用”，不读取私有接口、不展示伪数值。

## 2. 自动化验证

执行结果：XCTest 共 32 项，32 项通过，0 失败，0 跳过。

本段新增覆盖：

- 非有限值、负数和大于 1 的系统数值被拒绝。
- 首次读取正常音量不产生无意义 HUD。
- 接入电源、断开电源和跨入低电量阈值的事件映射正确。
- 连续音量变化保持同一 `system.audio` HUD，并更新为最新数值。
- 低电量可抢占展开操作，普通音量 HUD在用户操作时排队。
- 当前 Mac 可通过公开接口读取真实电池和音频数据。

构建结果：Debug App 构建成功，本地签名成功，`git diff --check` 通过。

## 3. 真实界面自审

| 验证项 | 结果 | 证据 |
|---|---|---|
| 系统电量一致性 | 通过 | NotchFlow 35%，macOS 系统设置 35% |
| 当前供电来源 | 通过 | NotchFlow 显示“电池”，与电脑当前未接电源一致 |
| 音量 HUD | 通过 | 调整后显示“音量 38%”与对应进度条 |
| 连续音量更新 | 通过 | 同一 `system.audio` HUD 原地刷新，不创建多个活动 |
| 静音 HUD | 通过 | 显示静音图标、“已静音”和 0% 进度 |
| 解除静音 HUD | 通过 | 恢复显示“音量 38%”及真实进度 |
| 普通系统窗口 | 通过 | 同时展示电量、供电、充电、音量、静音与亮度降级说明 |
| 原系统设置恢复 | 通过 | 验证后恢复为音量 38%、未静音 |
| 刘海高度与遮挡 | 通过 | 沿用统一顶部高度，只横向展开，不向下覆盖应用文字 |

## 4. 已修复问题

首次实测发现：部分输出设备的主声道与左右声道静音属性可能不完全一致。旧读取方式遇到第一个可读的 `false` 就停止，可能漏报静音。

修复后改为读取所有可读输出声道：只要任一声道为静音就显示静音；均为非静音才显示未静音。真实静音和解除静音复测均通过。

## 5. 真实电源插拔验收

产品经理已在真实 MacBook 上完成实体充电线验证：

1. 插入电源后，刘海正确显示电源接入/充电 HUD。
2. 拔出电源后，刘海正确显示电池供电 HUD。

至此，开发段 3.3 的自动测试、真实界面自审与实体硬件验收全部通过，可以进入开发段 3.4。
