# Paimon Pal 版本说明

## 0.3.0 (7) — 2026-08-30

- 菜单栏入口从通用药丸图标替换为派蒙头像专用模板图标，可随 macOS 菜单栏明暗自动着色。
- 保留待机 16 帧和其余动作 8 帧的原始节奏，在相邻关键帧之间增加轻量平滑合成，以 30fps 作为最低流畅度目标。
- 合成只在短暂过渡时运行，静止时不持续刷新，不额外常驻数十张中间帧。
- 待机编排增加低频自然挥手，不影响拖动、点击或刘海状态切换。
- 跨状态端点禁止双图叠加，避免出场、聆听和返回时出现双影。

## 0.3.0 (6) — 2026-08-30

- 修复用户允许语音识别权限后，系统在后台线程回调导致 Swift 6 主线程隔离检查触发 `SIGTRAP` 的闪退。
- 修复麦克风开始提供实时音频帧后，Audio Tap 在 Core Audio 实时队列触发同类线程隔离崩溃。
- 权限、音频和识别回调现在先在非界面线程安全接收，再显式切回主线程更新派蒙状态。

## 0.3.0 (5) — 2026-08-30

- 新增必须手动开启的“AI 对话实验模式”：麦克风收音、本机普通话识别、4B 本地模型回复和 P1 本地语音依次循环。
- 派蒙朗读期间停止收音，播放结束后等待 0.9 秒再恢复，降低把自己的声音再次识别为新问题的风险。
- 单次实验最多 12 轮或 10 分钟；可从菜单栏或设置随时停止，暂停应用、关闭语音或退出应用时也会停止。
- 原始录音不写入磁盘，不回退到联网语音识别；新增麦克风与语音识别的按需权限说明。
- 明确本模式不会自动训练本地 4B 模型；AI 与 AI 的对话只能作为待人工筛选的候选样本。

## 0.3.0 (4) — 2026-08-30

- 同机对比 Qwen3 1.7B 4-bit、Qwen3 4B 4-bit 与 Qwen3 4B Instruct 2507 4-bit，正式聊天模型升级为 4B Instruct 2507。
- 4B 指令版不再进入长时间隐藏思考；加强上下文指代、明确约束、纠错直答和现实能力边界。
- 模型仍按每次回复独立启动和退出，空闲时不常驻占用约 2.4GB 推理内存。

## 0.3.0 (3) — 2026-08-29

- 产品正式命名为 **Paimon Pal**，简称 **PP**。
- 更新应用显示名、设置、菜单栏、首次使用说明、通知、文件架提示和安装包名称。
- 为保留既有设置与本地数据，内部工程名、Bundle ID 与沙盒目录继续使用 `NotchFlow`。
- 派蒙工具扩展：支持用自然语言暂停、继续、取消和查询计时器，以及播放、暂停、上一首、下一首和查询 Apple Music 当前歌曲。
- 工具路由保持确定性白名单；普通聊天不会被误判为系统操作，工具失败会返回真实错误而不是假装成功。
- 修复 Apple Music 播放一段时间后“上一首”只回到本曲开头的问题，现在会稳定切回上一首歌曲。
- 内置用户选定的 P1“清脆反应感”原创幻想伙伴声线与离线 TTS；文字回复先显示，真实播放期间联动说话动画。
- 支持播报计时/窗口工具结果和计时结束提醒，新请求会打断旧语音，失败时保留文字结果。
- 设置新增“派蒙语音”开关和停止播放入口，本地语音不联网下载、不上传文本或音频。
- 取消语音层对每条回复强制添加“旅行者”；对用户针对助手用词和行为的反馈改为先直接承认并说明调整，不再误转成角色扮演话题。
- 修复 VoiceDesign 会按每句文本重新设计声线、导致同一次对话听起来像不同人的问题；改用已验收 P1 样音作为安装包内固定参考声纹。
- 物理刘海上的活动区向左预留菜单栏安全间距，避免遮挡右侧的系统图标与 iPhone 实时活动。
- 释放 1.7B 本地模型能力：复杂追问自动启用思考，简单对话保持快速回复；输出空间由 96 Token 分级提高到 192/512 Token。
- 会话上下文由拼接文本改为带用户/助手角色的结构化消息，窗口扩展到最近 8 条；提示词改为先直接回答，再保持派蒙式陪伴，禁止用套话和虚构实体动作代替答案。

## 0.3.0 (2) — 2026-08-29

- 新增派蒙主题的 Paimon Pal macOS 应用图标，用于个人课程作业展示。

个人课程作业桌宠版本，继续采用本机临时签名，尚未完成 Developer ID 正式签名与苹果公证。

### 新增

- 与物理刘海强关联的桌面宠物出场、待机、点击、聆听、回复和成功反馈动画。
- 内置 Qwen3 1.7B 级 4-bit 本地模型，离线生成简短中文陪伴回复。
- 独立连续聊天窗口，最多携带最近 6 条会话消息，支持清空且退出应用后不保留。
- 派蒙可启动倒计时，并打开设置、音乐、文件架、系统状态和计时器窗口。

### 修复与优化

- 修复聆听状态无法再次点击派蒙的问题。
- 修复小模型回忆前文时回避或编造内容的问题，回忆请求改为读取真实会话记录。
- 修复倒计时或音乐紧凑活动期间宠物无法出现的问题；响铃和重要提示仍保持优先。
- 将桌宠待机序列扩展到 16 帧，并降低持续抖动和切换闪烁。

### 当前边界

- 桌宠形象素材仅用于个人学习、课程作业和非商业演示，不包含公开分发或商业授权。
- 本地模型与桌宠素材会显著增加 App 和 DMG 体积。
- 当前只构建 Apple Silicon `arm64` 本机测试包。

## 0.2.0 (1) — 2026-08-27

本地 MVP 验收版本，尚未完成 Developer ID 正式签名与苹果公证。

### 新增

- 物理刘海贴合、悬停预览、点击展开和右键功能入口。
- Apple Music 播放信息、封面、进度与播放控制。
- 文件架复制暂存、导出、拖出、AirDrop、单项及全部清理。
- 电池、电源、音量和静音状态提示。
- 单倒计时、暂停/继续、结束提醒和异常重启恢复。
- 功能开关、菜单栏、暂停、登录启动、权限中心、多显示器与全屏策略。
- 首次启动说明和设置中的隐私/支持范围说明。

### 优化

- Apple Music 脚本复用与自适应轮询。
- 降低静默状态的电池读取和计时器无效刷新。
- 当前实机静默 CPU 约 0.66%，内存约 29.6 MB。

### 当前边界

- 音乐首版只验收 Apple Music，Spotify 暂缓。
- 亮度缺少稳定公开通用接口，因此不显示亮度数值。
- 当前 Release 为 Apple Silicon `arm64`。
- 正式签名、公证与 DMG 安装验证留到开发段 5.4。
