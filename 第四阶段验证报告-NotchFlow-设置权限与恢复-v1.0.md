# NotchFlow 第四阶段验证报告｜设置、权限与恢复

> 验证日期：2026-08-27  
> 开发分支：`phase/04-settings-permissions-recovery`  
> 完成分支：`phase/04-settings-permissions-recovery-complete`  
> 验证结论：通过，允许进入验收与发布准备阶段

## 1. 阶段交付

1. 设置与菜单栏：设置窗口、菜单栏快捷操作、开机启动、暂停与自动恢复。
2. 功能与权限：音乐、文件架、系统状态、计时器独立开关，以及自动化、通知、文件访问和增强 HUD 的真实状态说明。
3. 显示策略：内置屏、主显示器、指定显示器、三档全屏行为和无刘海屏幕微调。
4. 异常恢复：设置坏值修复、计时器状态恢复、文件架索引重建和权限撤回安全降级。

## 2. 自动化与构建

- XCTest：64 项通过，0 失败，0 跳过。
- Debug App：构建成功。
- 本地临时签名：成功。
- `git diff --check`：通过。
- 最终结果包：`/tmp/NotchFlow-Phase4-4.4-final2-20260827.xcresult`。

## 3. 真实验收摘要

| 开发段 | 真实验收结果 |
|---|---|
| 4.1 设置与菜单栏 | 设置、菜单栏入口、展开/收起、暂停、恢复和开机启动通过 |
| 4.2 功能与权限 | 四个功能独立关闭与恢复、权限中心显示通过 |
| 4.3 显示器与全屏 | 真全屏隐藏、退出全屏自动恢复且不残留错位面板 |
| 4.4 异常恢复 | 真实 App 强退重启后倒计时从约 `04:28` 连续恢复，未重置为 `05:00` |

## 4. 阶段内关键修复

- 修复鼠标悬停导致的反复伸缩和轻微形变。
- 将物理刘海各状态限制在不遮挡应用文字的顶部带内。
- 修复经典音乐布局缺少上一首按钮。
- 修复菜单栏可见性、暂停恢复和功能开关的运行态同步。
- 修复保留菜单栏的真全屏窗口识别。
- 增加非法设置写回、损坏计时清理、文件索引重建和跨重启状态恢复。

## 5. 未冒充完成的设备项

当前只有内置 MacBook 屏幕，因此以下项目继续标记为发布前硬件补验：

- 外接无刘海显示器连接、拔出与重新连接。
- 指定外接显示器消失后的主屏回退与重新连接恢复。
- 合盖后迁移到外接主显示器。

这些项目有自动化策略覆盖，但没有真实硬件结果，不写为人工通过。

## 6. 阶段结论

第四阶段 4.1—4.4 已完成开发、自动测试、真实 App 检查和产品经理人工验收。本机范围满足阶段出口，可以提交代码并创建完成分支；DMG、Developer ID 正式签名、公证与公开发布仍属于下一流程。
