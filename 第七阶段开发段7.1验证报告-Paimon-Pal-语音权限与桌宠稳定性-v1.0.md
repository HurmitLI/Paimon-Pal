# 第七阶段开发段 7.1 验证报告：Paimon Pal 语音、权限与桌宠稳定性 v1.0

验证时间：2026-09-03

验证环境：Apple Silicon MacBook Pro，安装版 Paimon Pal 0.5.0，Retina 缩放比 2

## 1. 本次问题与处理

| 用户反馈 | 根因 | 处理结果 |
|---|---|---|
| 文字先出现，等待较久后才开始说话 | 每句话都重新启动 Python、加载 TTS 模型并等待完整 WAV | 改为按需常驻的本地语音工作进程，模型预热后按 0.32 秒 PCM 块边生成边播放 |
| 已给 Paimon Pal 屏幕录制权限，当前窗口仍提示缺权限 | 窗口名由独立 `osascript` 进程读取，权限身份可能与 Paimon Pal 不一致 | 改由 Paimon Pal 本体的 Electron 捕获接口读取窗口名，再按系统窗口号合并；增加“重新检测”入口 |
| 桌面派蒙发虚 | 314px 单帧通过 CSS 百分比背景在 Retina 屏幕放大，并可能出现亚像素采样 | 改用按设备像素比预渲染的 Canvas；本机 216 CSS px 对应 432 物理像素，裁剪与落点均为整数 |
| 派蒙没事时持续抖动 | 16 帧待机动作每 1.76 秒无休止循环 | 改为先静止约 5.2 秒，再偶发播放一次待机动作；点击动作结束后同样先进入静止状态 |

## 2. 自动测试

- Node/Electron 全量测试：101 项通过，0 失败。
- Python 流式语音协议：模型加载 1.527 秒；首个音频块 367 毫秒；完整测试音频 3.2 秒、153,600 字节。
- 安装版端到端回归：4B 回答、1 分钟计时、桌宠拖动、小对话框、流式 TTS 均通过。
- 首版流式安装测试：首个音频块 194 毫秒；共 149,760 字节；至少一个音频块成功送达渲染层。该激进首块策略已在第 5 节按实机听感调整。
- 桌宠渲染：216×216 CSS 像素、432×432 Canvas 物理像素，符合 DPR 2；进入待机后连续 700 毫秒画面保持完全一致。
- DMG：约 147MB；App：约 344MB；签名校验及 `hdiutil verify` 均通过。

## 3. 权限边界与人工补验

新代码已消除“脚本进程没有继承 Paimon Pal 权限”的结构性问题。但当前应用没有 Developer ID，使用临时签名；新构建替换旧构建后，macOS 可能把旧授权绑定到旧的代码哈希。

本机安装新版后，系统当前仍返回 `screen_recording_permission_required`。因此还需要一次最终系统操作：在“系统设置 → 隐私与安全性 → 屏幕录制与系统录音”中关闭再开启 Paimon Pal，完全退出并重开应用，然后在“当前窗口”卡片点“重新检测”。这一步只能由当前登录用户确认，不能用自动测试伪造为通过。

## 4. 当前结论

- 语音延迟优化：通过。
- 桌宠清晰度与持续抖动修复：自动验证通过，主观清晰度仍受现有 314px 原始单帧素材上限影响。
- 屏幕录制权限代码修复：通过；当前安装版的系统授权刷新待用户完成一次最终确认。

## 5. 2026-09-03 实机反馈后的补充修复

### 5.1 开头音节异常

首版流式实现为了追求约 0.2 秒开口，在模型只有 0.32 秒语音上下文时就播放第一块。实机听感确认前一两个音节不自然。现将首段与后续分块统一提高到 0.80 秒上下文，并在第一块增加 25 毫秒淡入。

- 独立工作进程测试：首块 1,018 毫秒，完整音频 3.92 秒、188,160 字节。
- 最终安装版端到端测试：首块 427 毫秒、134,400 字节。
- 结论：仍明显快于整句生成后播放，但不再用音质不稳定的极短首块换取数字上的最低延迟。

### 5.2 小对话框抢占刘海控制台

桌宠透明窗口覆盖了物理刘海下方区域。原逻辑把这一区域的所有点击都解释为“打开派蒙对话”，导致小对话框打开后无法切换到完整控制台。现将停靠状态顶部 44px 定义为刘海点击区，并增加“小对话框 → 完整控制台”的显式状态切换。

自动回归结果：小对话框先正常打开；随后刘海接管成功，`assistantOnly=false`、`expanded=true`、小对话框隐藏、完整工具栏恢复显示。

### 5.3 屏幕录制权限

系统设置中曾显示 Paimon Pal 已开启，但当前临时签名程序仍拿不到窗口名，证实是旧代码哈希对应的失效授权记录。已只重置 Bundle ID `com.paimonpal.notchworkspace` 的 ScreenCapture 授权，其他应用权限未修改；系统列表现已把 Paimon Pal 显示为关闭，等待用户对当前安装版重新开启一次。

### 5.4 桌宠归位后残留对话气泡

原归位流程只保存桌宠停靠状态并播放消失动画，没有同步结束独立的小对话框，导致派蒙已经回到刘海后气泡仍停留在桌面。现将拖回刘海与程序化归位统一接入对话关闭请求：气泡退场、语音停止、输入焦点释放，随后桌宠完成停靠。

安装版自动回归已复现完整路径：桌面派蒙打开小对话框，从桌面坐标 `(1089, 313)` 拖至刘海停靠坐标 `(642, 33)`；归位后 `assistantOnly=false` 且对话面板为隐藏状态。全量测试仍为 101 项通过、0 失败。

### 5.5 点击时放大回缩与表情抖动

素材测量确认静止动画的角色可见高度为 236 源像素，而点击、聆听和说话动画分别达到 267、272 和 282 源像素；直接按整张 314×314 单元格绘制会在状态切换时产生 13% 以上的视觉放大。点击动画第 2 至第 6 帧的头部、身体和脚部还随动作整体向右偏移最多约 10 源像素，因此被感知为角色抖动。

现以静止帧的 236 像素高度和脚底 `y=302` 为统一视觉基准，在 Retina 画布预渲染阶段缩放各动作，并对点击表情的整体横移做逐帧抵消。安装版自动点击采样结果：静止状态可见高度 326 物理像素，12 次表情采样均为 325–326 物理像素；脚底基线始终为 415，头部水平重心波动由修复前约 4.23 物理像素降至约 2.12 物理像素（约 1.06 CSS 像素）。对话框仍正常打开，计时、4B 问答、拖动与归位回归均通过。
