# NotchFlow 第四阶段开发段 4.2 验证报告

> 报告版本：v1.0  
> 验证日期：2026-08-27  
> 当前分支：`phase/04-settings-permissions-recovery`  
> 结论：通过，可进入开发段 4.3

## 1. 实现范围

- 新增“通用 / 功能 / 权限”三分页设置中心。
- Apple Music、文件架、系统状态和计时器可独立开关并持久化。
- 关闭音乐和系统状态后停止轮询；关闭计时器后停止刷新但保留目标时间；关闭文件架后停止接收新拖入且不删除暂存副本。
- 已关闭功能不再出现于刘海右键菜单和普通功能窗口分段选择中。
- 权限中心展示辅助功能、Apple Music 自动化、通知和文件访问状态。
- 打开设置不请求权限；通知仅在用户点击“启用通知”时请求。
- macOS 亮度读写保持“当前不可用”的明确降级，不制造假数值。
- macOS 应用菜单“设置…”和 `⌘,` 与刘海右键入口统一打开同一设置窗口。

## 2. 自动化验证

最终结果包：`/tmp/NotchFlow-Phase4-4.2-final-20260827.xcresult`

- 测试总数：49
- 通过：49
- 失败：0
- 跳过：0
- `git diff --check`：通过

本段新增覆盖：

- 四个功能默认开启。
- 四个开关可分别写入并在重开后恢复。
- 停止计时器控制器时移除刘海活动，但不删除未完成计时状态。

## 3. 真实 Mac 验收

### 3.1 设置界面

- 产品经理确认设置窗口显示正常。
- “通用 / 功能 / 权限”三个分页正常显示，无文字裁切。

### 3.2 四个功能开关

- 关闭 Apple Music 后，刘海右键菜单中的“打开音乐窗口”消失。
- 只保留 Apple Music 时，文件架、系统状态和计时器入口全部消失，音乐入口保留。
- 恢复验收前状态后，四个功能入口全部重新出现。
- 验收过程没有申请或修改 Apple Music、通知、辅助功能和文件权限。

## 4. 权限与降级审核

- Apple Music 权限使用公开的 `AEDeterminePermissionToAutomateTarget`，参数 `askUserIfNeeded` 固定为 `false`，只读状态时不弹授权框。
- 辅助功能使用 `AXIsProcessTrusted()` 只读当前状态。
- 通知使用 `UNUserNotificationCenter.notificationSettings()` 读取状态，只在明确按钮操作后调用授权请求。
- 文件访问使用 App Sandbox 的用户选择读写权限，不申请全盘访问。
- 任一权限被拒绝时只降级相应功能，不作为应用崩溃或其他模块失败。

## 5. 验收后状态

- 标准测试 App 已重新启动。
- 四个功能均恢复为验收前状态；本机验收前未显式写入这些开关，因此当前使用默认的“全部开启”。
- 开机启动、暂停状态和权限状态未被修改。
- 没有安装新软件，没有创建云资源，没有上传任何数据。
