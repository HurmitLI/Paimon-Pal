# NotchFlow 第五阶段开发段 5.3 验证报告｜隐私、首次启动与版本资料

> 文档版本：v1.0  
> 验证日期：2026-08-27  
> 当前结论：通过，开发段 5.4 需先确认分发级别  
> 当前分支：`phase/05-acceptance-release-prep`

---

## 1. 本段完成内容

- 新增首次启动说明窗口，解释刘海操作、四个模块、权限和文件副本策略。
- 首次说明未完成时不启动 Apple Music 监听，确保说明窗口本身不触发自动化权限。
- “开始使用”保存完成状态；“稍后”不保存，下次启动继续显示。
- 设置新增“关于”页，可重新打开使用说明，并查看隐私、数据、版本与支持边界。
- 更新 README，新增隐私说明、版本说明和支持矩阵。

## 2. 隐私与数据核对

- App 不包含账号、云端同步、广告或分析功能。
- Swift 源码中未发现 `URLSession`、Network 连接、遥测、Firebase、Sentry 或 HTTP 地址。
- Release 二进制只链接苹果系统框架，没有第三方运行库。
- Apple Music 信息只在本机读取与控制。
- 文件架只处理主动拖入或选择的内容，副本位于 App Sandbox 的 Application Support 目录。
- 自动清理和手动删除只允许处理 NotchFlow 管理的副本，默认保留 24 小时。

## 3. 自动验证

- XCTest：66/66 通过，0 失败，0 跳过。
- 新增首次启动默认状态、完成持久化和重置测试。
- Release `arm64` 构建：通过。
- 本机临时签名与 `codesign --verify --deep --strict`：通过。
- 完成状态写入 `settings.onboarding.completed` 后可从新实例读取。

临时签名仍不等于 Developer ID 正式签名或苹果公证。

## 4. 人工验收

产品经理使用 5.3 Release App 完成：

1. 首次启动直接看到“欢迎使用 NotchFlow”。
2. 说明文字完整，可见“稍后”和“开始使用”，没有额外系统权限弹窗。
3. 点击“开始使用”后窗口关闭，完成状态成功保存。
4. 打开“设置 → 关于”，确认“使用说明、隐私与数据、版本与支持范围”显示正常。
5. 点击“打开说明…”，欢迎说明窗口可以再次打开。

结论：开发段 5.3 通过。

## 5. 下一段前置决策

开发段 5.4 可以先生成本机测试 DMG；若要正式对外分发，则还需要：

- 有效的 Apple Developer Program 账号。
- Developer ID Application 证书。
- notarytool 公证凭据。
- 明确 Intel `x86_64` 是否属于支持范围。

当前机器没有有效 Developer ID 身份，也未配置公证凭据，因此在产品经理确认分发级别前不执行正式发布动作。
