# NotchFlow 第二阶段验证报告｜交互底座

> 报告版本：v1.0
> 验收日期：2026-08-26
> 对应分支：`phase/02-interaction-foundation`
> 结论：第二阶段已通过自动化、真实运行和人工验收

## 1. 本阶段交付结果

- 建立正式 NotchFlow macOS App 与 XCTest 工程骨架。
- 完成物理刘海几何检测、顶部锚定和无刘海屏幕降级布局。
- 完成 S0–S5 刘海状态与 S6 普通功能窗口的交互底座。
- 完成活动优先级、临时 HUD 抢占/恢复、文件拖入抢占/恢复规则。
- 完成悬停展开、离开收回、单击两翼控制、点击外部收回。
- 完成标准动画和 macOS“减少动态效果”适配。
- S4 采用不向下覆盖应用内容的刘海两翼控制；S6 采用可移动、可关闭、不置顶的普通 macOS 窗口。

## 2. 自动化与工程检查

| 检查项 | 结果 | 证据 |
|---|---|---|
| XCTest | 通过 | 11/11 测试通过，`TEST SUCCEEDED` |
| 布局契约 | 通过 | 全部物理刘海状态高度一致，顶部锚点不变 |
| 状态机 | 通过 | 优先级、HUD 排队/恢复、文件拖入抢占/恢复通过 |
| 编译 | 通过 | Debug App 构建成功 |
| 签名校验 | 通过 | App `valid on disk` 且满足 Designated Requirement |
| 真实启动 | 通过 | 正式 App 进程已运行 |
| 代码格式检查 | 通过 | `git diff --check` 无错误 |

测试命令：

```bash
xcodebuild -project NotchFlow.xcodeproj \
  -scheme NotchFlow \
  -configuration Debug \
  -destination 'platform=macOS' \
  -derivedDataPath build/NotchFlowTestData \
  CODE_SIGNING_ALLOWED=NO test
```

## 3. 真机几何与窗口检查

| 场景 | 实测结果 |
|---|---|
| S0 静默状态 | `209 × 39pt`，`X=651`，`Y=0`，窗口层级 26 |
| S4 两翼展开 | 宽度从 `209pt` 扩展到 `337pt`，高度保持 `39pt` |
| 点击外部 | 宽度从 `337pt` 恢复到 `209pt` |
| 键盘焦点 | 点击刘海前后前台 App 保持不变，刘海不抢占焦点 |
| S6 功能窗口 | 普通层级 0，可移动、可缩放、可关闭，不始终置顶 |

## 4. 产品经理人工验收记录

| 验收项 | 结果 | 人工确认 |
|---|---|---|
| S0 清除活动后恢复纯黑刘海 | 通过 | 已确认 |
| S1 模拟紧凑活动 | 通过 | 绿色图标和“模拟活动”正常显示 |
| S2 临时 HUD | 通过 | “音量 50%”显示后自动恢复 S1 |
| S3 悬停预览 | 通过 | 展开稳定，无反复伸缩和闪烁 |
| S4 两翼快捷控制 | 通过 | 两侧按钮可点击，不向下遮挡文字 |
| S5 文件拖入反馈 | 通过 | 拖入出现绿色反馈，拖离自动恢复 |
| S6 普通功能窗口 | 通过 | 窗口形态正常 |
| 全屏 Space 定位 | 通过 | 位置正常，无额外黑块 |
| 全屏悬停和收回 | 通过 | 展开稳定，移出后成功收回 |
| 标准动画 | 通过 | 展开、收回稳定 |
| 减少动态效果 | 通过 | 无弹性回弹、拉伸或过冲 |

## 5. 验收期间发现并修复的问题

1. SwiftUI 悬停事件在窗口改变尺寸时重复进出，导致反复展开收回。已改为窗口控制器统一监听指针坐标。
2. `@Published` 的新状态发布时使用了旧状态计算窗口几何。已改为直接使用新发布的状态。
3. S4 中央大浮层遮挡应用文字。已将产品规格和实现改为等高的刘海两翼控制。
4. 模拟 HUD 保留时间太短，人工验收难以观察。正式默认值保持 1.5 秒，仅将测试入口延长为 3 秒。
5. 外部点击监听错用了窗口内坐标。已改为用全局鼠标坐标与面板屏幕坐标比较，真机复测通过。

## 6. 本阶段边界与后续项

以下能力未在第二阶段开发，不影响本阶段交互底座验收：

- Apple Music 正式数据、播放控制和封面。
- 文件复制、暂存、管理和 AirDrop。
- 电池、音量、计时器等真实业务模块。
- 设置页、持久化、权限引导和发布包。
- PRD 5.3 中的 Esc 收起和双指上下滑手势未列入本阶段技术文档交付范围，当前未验收，需在后续阶段单独排期。
- 外接屏、合盖模式和多屏矩阵留待后续兼容性阶段验收。

## 7. 阶段结论

第二阶段出口条件已满足，可进行本地 Git 分支留档。留档后才进入下一阶段，不在本报告中提前开发正式功能。
