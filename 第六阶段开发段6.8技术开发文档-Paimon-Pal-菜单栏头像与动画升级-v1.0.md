# 第六阶段开发段 6.8 技术开发文档：Paimon Pal 菜单栏头像与动画升级

## 1. 本段目标

本段不扩展聊天、工具或模型能力，只处理两个视觉问题：

1. 将 macOS 右上角菜单栏中的通用药丸入口改为派蒙头像。
2. 在不显著增加 4B 本地模型并行负担的前提下，提升桌宠待机丰富度并确保播放稳定。

## 2. 开发前现状

| 动作 | 关键帧数 | 原始播放速度 |
|---|---:|---:|
| 待机 | 16 | 10 fps |
| 睡醒、返回、成功庆祝 | 8 | 8 fps |
| 飞出、点击反应 | 8 | 10 fps |
| 聆听、说话 | 8 | 6 fps |

因此用户看到的“帧数低”不是错觉。直接把 8 帧动作调到 30 或 60 fps 只会让动作加速，不会变顺；预生成大量 512×512 中间帧又会与 4B 模型抢占内存。

## 3. 技术方案

### 3.1 菜单栏头像

- 新增 36×36 px @2x 派蒙头像 PNG，实际显示尺寸为 18×18 pt。
- 最终采用用户确认的 C 方案：平面彩色头像，只保留金色光环、白色短发、蓝紫眼睛、深蓝星形发饰和围巾尖角。
- 使用 `NSImage.isTemplate = false` 保留角色颜色；透明背景确保蓝色、深色或浅色菜单栏下都没有方形底板。
- 资源缺失时保留系统人像图标兜底，避免菜单栏入口消失。

### 3.2 动画稳定性

- 原始关键帧数和动作总时长保持不变。
- 每次只绘制一张透明关键帧，不再同时叠加前后两张角色图。
- 继续使用已验收的角色尺寸与脚底锚点，防止双影、闪烁和跳缩。
- 本段不把 8 张关键帧虚构为 30 或 60 张；进一步提升动作流畅度需要重新制作更多真实中间帧，留待素材升级阶段。

### 3.3 待机丰富度

- 保留原有“安静停留 → 呼吸眨眼 → 长停留”。
- 每完成两次安静循环，低频播放一次自然挥手，随后回到第一待机帧。
- 挥手时仍处于待机状态，不锁定拖动，不打开对话框，不改变刘海活动优先级。

## 4. 验收标准

1. 菜单栏显示可识别的彩色派蒙头像，与旁边 macOS 图标大小协调。
2. 深色与浅色菜单栏下保留角色颜色，且不带方形底板或黑色方块。
3. 待机、点击、聆听、说话期间不因双帧透明叠加产生闪烁或重影。
4. 出场、状态切换和返回不产生双影、持续闪白、突然放大或缩小。
5. 待机期间可低频自然挥手，且拖动、点击和聊天行为不受影响。
6. 全量自动测试、Release arm64 构建、资源打包和单实例安装通过。

## 5. 本段边界

- 没有生成 60 张独立绘制帧，也不再用透明度合成冒充真实中间帧。
- 没有修改用户已验收的派蒙主体立绘、尺寸和透明边缘。
- 没有在后台持续生成图片，也没有改动本地模型或语音链路。
