# NotchFlow 第三阶段开发段 3.2 验证报告｜文件架与 AirDrop

> 验证日期：2026-08-27
> 开发分支：`phase/03-core-modules`
> 结论：通过；真实拖入与明确导出闭环均已完成

## 1. 自动化结果

- Xcode 编译、测试和本机签名启动通过。
- 第三阶段最终全量 XCTest 共 40 项，40 项全部通过。
- 文件架测试覆盖：
  - Finder `public.file-url` 数据解码。
  - 真实 `NSItemProvider` 进入控制器后的复制闭环。
  - 原文件保留。
  - 同名副本使用 `(2)` 避让且不覆盖。
  - 文件夹及嵌套文件复制。
  - 索引与磁盘不一致时自动修复。
  - 24 小时清理只删除 NotchFlow 管理的副本。
  - 不存在的来源返回失败且不生成伪记录。
  - 明确导出会复制内容、保留文件架副本和 Finder 原文件。
  - 目标目录重名时使用 `(2)` 避让，不覆盖已有文件。

## 2. 真实界面与系统能力自审

| 验证项 | 实际结果 | 结论 |
| --- | --- | --- |
| 文件架窗口 | 普通 macOS 窗口可移动、缩放、最小化和关闭，不置顶遮挡其他 App | 通过 |
| 文件信息 | 显示名称、类型、大小、Quick Look 缩略图和暂存时间 | 通过 |
| 选择文件导入 | 三个真实 Markdown 文件复制进入 App 容器；源文件仍存在 | 通过 |
| 同名避让 | 第二次导入自动保存为 `第二阶段技术开发文档-NotchFlow-交互底座-v1.0 (2).md` | 通过 |
| 内容一致性 | 源文件与暂存副本 inode 不同、文件大小一致、`cmp` 内容一致 | 通过 |
| 单击打开 | 单击文件卡片后由系统打开对应暂存副本，界面返回成功提示 | 通过 |
| Finder 定位 | “在 Finder 中显示”能打开 App 的 `Items` 目录并选中对应副本 | 通过 |
| AirDrop | 系统原生 AirDrop 面板成功打开并显示可选设备；未选择设备，随后取消 | 通过 |
| 删除保护 | 删除弹窗明确显示“仅删除 NotchFlow 副本”和“Finder 原文件不会被删除” | 通过 |
| 重启恢复 | 重启 NotchFlow 后三个暂存副本及索引全部恢复 | 通过 |
| 磁盘真相修复 | 将误导入的 App 副本移出暂存目录后，重启自动修复索引并恢复正确数量 | 通过 |
| 拖入数据链路 | 使用 Finder 同格式 `NSItemProvider` 完成文件 URL 读取、复制和源文件保护集成测试 | 通过 |
| Finder 拖入刘海 | 产品经理使用真实鼠标将 PRD 拖入刘海，文件架从 3 项变为 4 项并显示新 PRD | 通过 |
| 明确导出 | 第一行新增“导出…”按钮；AI 选择独立临时文件夹后成功导出 PRD，界面明确提示文件架副本仍保留 | 通过 |
| 导出内容一致性 | 导出 PRD 与 Finder 原 PRD 的 SHA-256 均为 `bcaf2b8141257bbe4601b05008365ae4e96e4becfafc4a98326a71034ea210b2` | 通过 |
| 拖出快捷手势 | 仍保留整行拖到 Finder 的快捷方式；电脑操作工具无法稳定跨 App 保持鼠标按住，不以其自动化结果冒充现场证据 | 保留能力，不作为唯一出口 |

## 3. 自审中发现并修复的问题

1. 文件列表最初只有文件类型图标，没有显示 PRD 要求的创建时间。现已改为 Quick Look 缩略图，并显示“暂存于”时间。
2. AirDrop 图标最初使用了容易误解的音频投送图标。现已改为无线传输图标，并保留明确的 `AirDrop` 无障碍名称。
3. 初版拖入入口直接接收 SwiftUI `URL`，不能明确覆盖 Finder 的 `public.file-url`。现已改为 macOS 原生 `NSItemProvider` 文件 URL 接收，并新增真实 Provider 集成测试。
4. 文件卡片原本只用无文字小图标暗示拖出，产品经理反馈“不明白”。现已改为明确的“导出…”按钮，同时保留整行拖到 Finder 的快捷手势。
5. 新增失败输入测试，确保读取失败时不生成伪成功索引。
6. 首次真实导出发现沙盒只有“用户所选文件只读”权限，导致目标目录复制被系统拒绝。现已收紧为仅对用户主动选择的目录启用读写权限，并完成修复后真实导出。

## 4. 安全与数据边界

- 文件只复制到 NotchFlow 沙盒容器的 `Application Support/NotchFlow/FileShelf/Items`。
- 导入、主动删除和 24 小时自动清理都只操作 App 管理的副本。
- AirDrop 只唤起系统面板，不自动选设备、不自动发送。
- “导出…”只有在用户主动选择目标文件夹后才复制；重名自动避让，不覆盖已有文件。
- 当前保留三个真实暂存副本，便于后续回归；它们不是 Finder 原文件。
- 一次误导入的 `Documents` 副本已移动到废纸篓：`NotchFlow-validation-Documents-copy-20260826`；源目录未受影响。
- 本次拖拽诊断临时目录已移动到废纸篓：`NotchFlow-drag-validation-20260827`，可恢复。

## 5. 开发段结论

开发段 3.2 的真实拖入、文件复制、索引、同名避让、缩略图与时间、打开、明确导出、Finder 定位、AirDrop、删除保护、自动清理和重启恢复均已完成。产品经理不再需要识别无文字的拖出图标；“导出…”已成为可发现、可验证的主路径，整行拖出继续作为快捷能力保留。
