# NotchFlow 第三阶段技术开发文档｜四个核心模块

> 文档版本：v1.0
> 更新日期：2026-08-27
> 对应 PRD：阶段 2｜四个核心模块
> 前置留档：分支 `phase/02-interaction-foundation-complete`，提交 `6813f78`
> 当前开发分支：`phase/03-core-modules`

## 1. 阶段目标

在已通过验收的刘海交互底座上，依次接入四个真实业务模块：

1. Apple Music 音乐控制。
2. 文件架与系统 AirDrop。
3. 电池、电源和音量系统状态。
4. 单计时器闭环。

每个开发段完成后立即停止，由 AI 给出一项一项的人工验证指令；产品经理确认后才进入下一段。

## 2. 范围边界

### 本阶段开发

- 真实系统数据与真实操作，不用模拟数据冒充验收。
- 刘海 S1–S5 状态与 S6 普通功能窗口的业务内容。
- 文件暂存索引和计时器的本地恢复能力。
- 与本阶段功能直接相关的渐进权限说明。

### 本阶段不开发

- 完整设置页、开机启动、多显示器设置和权限中心。
- 签名、公证、DMG 与对外发布。
- 多计时器、音频可视化、封面强调色和其他 P1 能力。
- 依赖私有 API 的屏幕亮度伪数据。
- Spotify 正式适配：根据已确认的阶段决策暂缓，不计入当前阶段出口。

## 3. 共用技术契约

- 保持 Swift + SwiftUI + AppKit 模块化单体，不增加第三方依赖。
- 四个模块通过明确 Protocol 与 `ActivityCoordinator` 交互，不把系统 API 写进刘海 UI。
- 优先级继续使用已验收状态机：文件拖入 > 重要系统状态 > 用户操作 > 计时器 > 音乐 > 普通 HUD。
- 用户可见错误使用简明中文；不展示 AppleScript 调用栈、完整文件路径或账号信息。
- 单一模块失败不影响其他模块和刘海基础交互。

## 4. 开发段 3.1：Apple Music

> 当前状态：已完成（2026-08-26）
> 验证留档：`第三阶段开发段3.1验证报告-NotchFlow-Apple-Music-v1.0.md`

### 开发范围

- 检测 Apple Music 是否安装、运行及当前播放状态。
- 读取歌曲名、艺人、专辑、时长、当前进度和可用时的封面。
- 支持播放/暂停、上一首、下一首和跳转 Apple Music。
- 能力允许时支持拖动进度；失败时自动降级为只读进度。
- 播放时创建真实音乐活动；播放器退出时清除活动。
- 刘海左翼提供上一首和播放/暂停，右翼提供下一首和完整音乐窗口入口。

### 自动验收

- 播放快照解析、时间边界、播放状态映射和失败降级单元测试通过。
- Apple Music 未运行、暂停、播放和退出都是明确状态，不崩溃。

### 人工验收

AI 引导产品经理逐项确认：实时信息、播放/暂停、上一首/下一首、进度、普通音乐窗口和播放器退出降级。

## 5. 开发段 3.2：文件架与 AirDrop

> 当前状态：已完成（2026-08-27）
> 验证留档：`第三阶段开发段3.2验证报告-NotchFlow-文件架与AirDrop-v1.0.md`

### 开发范围

- 从 Finder 或其他 App 拖入文件/文件夹，复制至 App 自有暂存目录。
- 索引显示名称、类型、大小、缩略图和创建时间。
- 支持打开、拖出、单个删除、清空暂存副本和系统 AirDrop。
- 每行提供明确的“导出…”按钮；用户选择目标文件夹后复制副本，重名自动避让，同时保留整行拖出快捷手势。
- 同名文件不覆盖；操作失败不生成伪成功记录。
- 默认 24 小时清理，只删除 App 创建的暂存副本。

### 人工验收

拖入一个真实文件，依次验证列表、打开、拖出、AirDrop 面板、删除副本与原文件保护。

产品经理已用真实鼠标完成 Finder → 刘海拖入，文件架正确新增 PRD。产品经理反馈无文字拖出图标难以理解后，主路径已改为明确的“导出…”按钮；AI 完成真实目标文件夹导出和内容哈希核对，整行拖出仍作为快捷能力保留。

## 6. 开发段 3.3：系统状态

> 当前状态：已完成（2026-08-27）
> 验证留档：`第三阶段开发段3.3验证报告-NotchFlow-系统状态-v1.0.md`

### 开发范围

- 监听电源接入/断开、电量、充电状态和低电量。
- 监听默认输出设备音量和静音状态。
- 连续调整音量时更新同一 HUD，不重复入场。
- 屏幕亮度保持“公开 API 有限通过”，当前不显示不可靠数值。

### 已实现技术路径

- `IOKit Power Sources` 读取电量、供电来源与充电状态，无需额外权限。
- `Core Audio` 读取默认输出设备主音量；主声道不可用时回退左右声道平均值。
- 静音同时检查可读的主声道与左右输出声道，任一声道静音即判定静音。
- 音频每 200ms 刷新，电池每 1 秒刷新；连续变化复用 `system.audio` HUD，并刷新同一退出计时。
- 所有数值进入 UI 前必须通过 `0...1` 有限数校验；异常值不显示。
- 系统功能窗口展示当前真实读数，并明确标记屏幕亮度当前不可用。

### 已完成验收

- 当前 Mac 通过公开接口读取电量、供电来源、音量与静音状态。
- NotchFlow 电量 35% 与 macOS 系统设置电量 35% 一致。
- 音量 38% HUD、同一 HUD 原地刷新、进度条、静音及解除静音均完成真实界面自审。
- 测试后系统声音已恢复到原状态：音量 38%，未静音。
- 全量 XCTest 32/32 通过，Debug App 构建与本地签名通过。

### 真实电源验收

- 产品经理插入电源后，刘海正确显示接入/充电 HUD。
- 产品经理拔出电源后，刘海正确显示电池供电 HUD。
- 开发段 3.3 的真实硬件验收已闭环。

### 人工验收

逐项验证插拔电源、系统电量对比、音量连续调整和静音。

## 7. 开发段 3.4：计时器与阶段出口

> 当前状态：已完成（2026-08-27）
> 验证留档：`第三阶段开发段3.4验证报告-NotchFlow-计时器-v1.0.md`

### 开发范围

- 快速选择 5、10、15、25、30、60 分钟和自定义时长。
- 启动、暂停、继续和结束单计时器。
- 使用目标结束时间计算剩余时间，避免睡眠后漂移。
- 应用重启后恢复未结束计时器。
- 结束时创建刘海提醒和系统通知；权限拒绝不影响刘海提醒。

### 已完成验收

- 快捷时长、自定义时长、启动、暂停、继续、手动结束和完成确认已接入普通计时器窗口。
- 紧凑刘海显示剩余时间；最后 60 秒改用橙色强调，不闪烁。
- 展开刘海可直接暂停、继续或确认结束，并可跳转完整计时器窗口。
- 计时状态按目标结束时间计算；暂停与重启恢复已完成自动测试和真实 App 复测。
- 3 秒真实倒计时结束后正确显示“计时结束”，确认后立即收起，不残留 `00:00`。
- 产品经理已完成人工验收：快捷启动、紧凑倒计时、展开控制、暂停、继续、完整窗口和手动结束均正常。
- 第三阶段最终全量 XCTest 40/40 通过，Debug App 构建、本地签名和 `git diff --check` 通过。

### 阶段出口

- 四个核心模块均完成自动化与人工验收。
- 一个模块不可用时，其他模块仍可使用。
- 状态优先级和恢复顺序符合 PRD。
- 文件与计时器数据在 App 重启后可恢复。
- 真实 App 构建、签名、启动、XCTest 和人工回归均通过。

## 8. 人工验收规则

- AI 每次只给一个明确操作，说清“去哪里、点什么、预期看到什么”。
- 产品经理只需照做、反馈现象和确认。
- 验收失败时，AI 先定位与修复，不降低标准、不删除失败测试。
- 开发段验收通过后，才进入下一段。
