# 第六阶段开发段 6.3 验证报告：Paimon Pal 动画一致性

## 1. 阶段目标

统一待机、点击、聆听、说话与成功反馈动作的角色视觉高度、横向中心和脚底锚点，消除不同素材画布比例造成的状态切换跳变。

## 2. 实现方案

- 在去除生成素材中的假刘海后，测量每帧有效透明像素边界。
- 使用各动作“单帧角色高度中位数”计算固定缩放，避免动作本身的上下移动被误算为角色大小。
- 同一动作所有帧共用一个固定变换，不逐帧缩放，因此不会产生呼吸式大小抖动。
- 使用动作最低脚底位置统一落点，同时保留动作内部原有的轻微上下浮动。
- 统一输出为 512×512 标准画布。
- 待机、点击、聆听、说话和成功反馈采用标准画布；探头、飞出和钻回保留原始位移过程。
- 探头、飞出和钻回也输出标准画布，并使用相邻状态端点校准；位移轨迹保留，但不再沿用放大后的原始画布比例。
- 对原始探头素材自带的近景镜头增加确定性缩放补偿：探头为 80%，飞出按 80% → 86% → 88% → 89% → 89% → 92% → 97% → 100% 平滑回到正常大小。
- 补偿围绕有效内容的水平中心与脚底锚点进行，既统一刚露头的头部大小，也不改变钻出刘海的垂直路径。
- 完整身体状态统一使用同一 SwiftUI 垂直偏移。

## 3. 自动测量结果

运行时导出的全帧有效像素测量：

| 动作 | 首帧高度 | 全帧高度中位数 | 全帧高度范围 | 最小边距（左/右/上/下） |
| --- | ---: | ---: | ---: | ---: |
| 待机 | 332px | 333px | 332–336px | 128/128/145/24px |
| 点击反馈 | 331px | 333px | 318–335px | 95/87/112/24px |
| 侧耳聆听 | 332px | 333px | 331–337px | 105/112/111/23px |
| 说话 | 334px | 334px | 334–335px | 101/104/121/23px |
| 成功反馈 | 332px | 332px | 332–334px | 72/93/95/24px |

五组动作的高度中位数仅相差 2px，小于 1%；全部帧的四边最小边距均大于 0，没有角色贴边或裁切。点击反馈的 318–335px 高度变化来自原动作姿态收缩，同一动作始终使用固定缩放，不是运行时大小跳变。

转场端点测量：

| 连接位置 | 前一帧高度/脚底边距 | 后一帧高度/脚底边距 |
| --- | ---: | ---: |
| 探头末帧 → 出场首帧 | 207px / 77px | 209px / 76px |
| 出场末帧 → 待机首帧 | 332px / 24px | 332px / 24px |
| 待机首帧 → 回程首帧 | 332px / 24px | 333px / 23px |

三个连接点均处于 1–2px 抗锯齿误差内，显示高度分别约为 58px、93px 和 94px，不再出现回程首帧约 111px 的放大。

露头近景补偿测量：原始探头阶段头部最大宽度约 245–246px，补偿后约 196–205px；待机动作约 195–197px。第一瞬间的头部尺寸已回到待机基准附近，同时飞出末帧仍为 100% 并与待机首帧精确连接。

## 4. 自动测试与运行验证

- Xcode 自动测试：101/101 通过，0 失败，0 跳过。
- 真实待机与聆听素材的角色高度、最低脚底锚点测试通过。
- 正方形与竖向合成画布的通用标准化测试通过。
- 五种完整身体动作均进入标准化路径。
- 三段转场动作的相邻端点尺寸与脚底锚点测试通过。
- 探头/飞出补偿曲线映射、合成帧缩放尺寸与脚底锚点保持测试通过。
- 冷启动自动进入聆听时，极简输入框正常出现并获得输入焦点。
- `Esc` 自动关闭输入框验证通过。
- 开发版构建、签名与单实例启动通过。
- 最新构建已更新至 `/Applications/Paimon Pal.app`；签名验证、构建产物二进制一致性和单进程运行检查通过。

## 5. 双实例回归修复

- 用户现场确认曾同时运行开发版与 `/Applications/Paimon Pal.app` 旧安装版；两个版本分别响应拖动、点击和对话窗口，造成桌面宠物重影、拖动失效及旧版中央大对话框重新出现。
- 新版启动时通过系统文件互斥锁原子占用唯一实例；第二个相同 App 无论从开发目录还是“应用程序”启动，均在桌宠窗口初始化前自行退出。
- 正向实测：开发版运行时再次启动安装版，最终仅保留开发版一个进程。
- 反向实测：安装版运行时再次启动开发版，最终仅保留安装版一个进程。
- 原安装版已保存在 `build/installed-app-backups/Paimon Pal-before-single-instance-20260829-2250.app`，当前 `/Applications/Paimon Pal.app` 已更新为本阶段新版。

## 6. 最终人工验收

只需观察一次完整链路：

1. 普通待机。
2. 单击派蒙，进入侧耳聆听。
3. 输入“你好”并发送，进入说话与成功反馈。
4. 回复结束后恢复普通待机。
5. 将派蒙拖回刘海，观察钻回动画。

通过标准：派蒙刚露头时不再出现偏大的头；从刘海出场、进入待机、切换聆听/回答以及钻回刘海时，整体大小与脚底位置自然连续，不再出现转场或聆听状态突然放大的割裂感。

当前状态：最终主观视觉验收已通过。用户确认刚露头的头部尺寸与待机状态一致，本开发段完成。
