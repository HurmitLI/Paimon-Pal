# NotchFlow 第一阶段技术可行性验证报告 v1.0

> 报告日期：2026-08-26  
> 当前状态：**第一阶段已完成，结论为条件通过**  
> 阶段范围：仅验证 macOS 原生底层能力，不代表正式产品 UI 已开发

## 1. 验证环境

| 项目 | 当前环境 |
|---|---|
| 设备 | MacBook Pro（Apple M5，24 GB） |
| 系统 | macOS 26.6.2（25G83） |
| 内置屏幕 | 3024 × 1964 Retina；系统逻辑尺寸 1512 × 982 pt |
| 架构 | arm64 |
| Xcode | 26.6（17F113） |
| macOS SDK | 26.5 |
| Apple Music | 已安装 |
| Spotify | 已安装 1.2.97.270；产品经理决定本阶段跳过实测 |
| 外接无刘海显示器 | 未连接，当前环境无法实机验证 |

## 2. 已建立的实验工程

- 原生 macOS App：Swift 6 + SwiftUI + AppKit
- 顶部浮层：`NSPanel`
- 屏幕能力：`NSScreen.safeAreaInsets`、顶部左右辅助区域
- 音乐能力：Apple Events / AppleScript 适配层
- 系统状态：IOKit 电源信息、CoreAudio 默认输出音量
- 文件能力：沙盒授权文件读取、Application Support 暂存、拖出、系统 AirDrop 分享
- 自动化测试：XCTest
- 第三方依赖：0

## 3. 自动化验证结果

执行结果：`TEST SUCCEEDED`，共 6 项通过、0 项失败。

| 验证项 | 结果 | 证据 |
|---|---|---|
| 物理刘海区域推导 | 通过 | 单元测试覆盖辅助区推导逻辑 |
| 无刘海回退逻辑 | 通过 | 单元测试覆盖安全区和辅助区缺失场景 |
| 非法几何数据保护 | 通过 | 不生成负宽度刘海区域 |
| Apple Music 安装检测 | 通过 | 当前系统能定位 `com.apple.Music` |
| 电池与音量读取 | 通过 | 当前 Mac 能通过 IOKit 与 CoreAudio 返回数据 |
| 文件沙盒内复制 | 通过 | 测试文件复制成功且内容一致 |

## 4. 当前 Mac 的刘海检测证据

系统实时读取结果：

```text
屏幕：Built-in Retina Display
frame：1512 × 982 pt
safeAreaInsets.top：32 pt
左辅助区：x=0, y=950, w=665, h=32
右辅助区：x=850, y=950, w=662, h=32
推导物理刘海：x=665, y=950, w=185, h=32
```

结论：**当前 Mac 可以使用公开的 `NSScreen` 数据识别物理刘海的位置和尺寸。**

## 5. 五组能力的阶段结论

| 能力 | 自动化结论 | 仍需人工确认 |
|---|---|---|
| 屏幕几何与物理刘海 | 通过 | 人工验收已确认浮层与实体刘海位置正常 |
| NSPanel 顶部浮层 | 通过 | 定位、点击、展开/收起、全屏 Space 与不抢焦点均已通过 |
| Apple Music | 通过 | 权限、信息读取、播放/暂停、上一首和下一首均已人工验证 |
| Spotify | 未验证，不计入本阶段出口 | 产品经理确认 Apple Music 结果已足够，本阶段主动跳过 Spotify |
| 电池状态 | 通过 | 电量、充电状态、电源类型和预计充满时间均与系统一致 |
| 输出音量 | 通过 | App 显示 50%，系统读取同为 50% |
| 屏幕亮度 | 有限通过 | 无稳定公开读写 API；本阶段拒绝采用私有接口 |
| 文件暂存与拖出 | 通过 | Finder 拖入、App 暂存与拖回 Finder 均已人工验证 |
| AirDrop | 通过 | 用户已确认成功弹出系统 AirDrop 面板；未实际发送文件 |
| 无刘海外接屏 | 回退算法通过 | 缺少外接屏，等待有设备时补实测 |

## 6. 已确认的技术边界

1. 刘海尺寸不写死，必须每块屏幕实时读取。
2. 外接无刘海屏使用顶部居中的回退尺寸，不伪造物理刘海。
3. Apple Music / Spotify 控制依赖用户主动授权自动化权限。
4. Spotify 未安装时必须明确显示“不可测/未安装”，不能当作失败或通过。
5. 屏幕亮度没有适合正式产品的稳定公开读写接口。后续功能规格需要降级或另行决策，不能偷偷使用私有 API。
6. AirDrop 只能调用系统分享面板，App 不绕过用户直接发送文件。
7. 当前签名是本机实验用临时签名，不是正式发布签名。

## 7. 阶段出口条件

第一阶段以下两项已经满足：

- 自动化测试 6/6 通过。
- 产品经理已完成本机可执行项目的逐项人工验收，并确认 Apple Music 验证足以代表本阶段音乐能力。

阶段结论：**条件通过，可以结束第一阶段；下一阶段仍需产品经理明确下令后才启动。**

条件与边界：

- Spotify 本阶段未实测，不得对外宣称已支持；是否保留为正式 MVP P0，进入正式功能开发前再确认。
- 无刘海外接显示器缺少实机，当前只有回退算法测试通过，不得宣称外接屏已完成兼容。
- 亮度没有稳定公开读写 API，结论保持“有限通过”，正式功能不能显示估算值。

## 8. 人工验收修正记录

### 2026-08-26：顶部浮层首次位置偏低

- 用户实测：黑色实验浮层被系统放到了摄像头黑块下方。
- 原因：macOS 自动把无边框 `NSPanel` 约束到菜单栏安全区以下，窗口坐标为 `Y=33 pt`。
- 修正：实验窗口取消系统的菜单栏位置约束，同时让 SwiftUI 内容忽略顶部安全区。
- 修正后自动读取：实验浮层窗口坐标为 `X=627, Y=0, W=257, H=42 pt`，已经贴到物理屏幕顶边。
- 用户复验：已确认“现在正常”。
- 当前状态：**物理刘海定位人工验收通过**。

### 2026-08-26：刘海左右可见区域无法点击

- 用户实测：展开/收起功能可用，但只能点击刘海下方，左右圆点所在区域不能触发。
- 原因：设置 `isFloatingPanel` 后，系统把窗口层级重置为 `3`，低于菜单栏的 `24/25` 层；界面可见但点击事件被菜单栏截获。
- 修正：在浮动属性设置完成后，将实验窗口最终层级设为 `26`。
- 修正后自动读取：实验浮层为 `layer=26, X=627, Y=0, W=257, H=42 pt`。
- 用户复验：已确认“圆点可以点击”。
- 当前状态：**刘海区域点击与展开/收起人工验收通过**。

### 2026-08-26：全屏应用行为

- 用户使用 VS Code 全屏实测。
- 验收结果：已确认“全屏正常”。
- 当前状态：**全屏空间持续显示与交互人工验收通过**。

### 2026-08-26：点击浮层抢占前台应用

- 用户在 VS Code 上方展开浮层后，系统读取到的前台应用变成 `com.notchflow.FeasibilityLab`。
- 验收结论：首次焦点测试未通过。
- 原因：实验 `NSPanel` 被允许成为键盘主窗口。
- 修正：禁止浮层成为 Key/Main Window，并让承载视图只通过 `acceptsFirstMouse` 接收首次鼠标点击。
- 用户按要求先点击 VS Code，再点击刘海圆点；系统随后读取到的前台应用仍为 `Code`（`com.microsoft.VSCode`）。
- 当前状态：**不抢占当前应用焦点人工验收通过**。

### 2026-08-26：Apple Music 信息读取

- 用户在“音乐”App 播放歌曲后，通过实验 App 执行读取。
- 用户确认界面已显示歌曲名称和艺人。
- 当前状态：**Apple Events 权限与 Apple Music 播放信息读取人工验收通过**。

### 2026-08-26：Apple Music 播放与暂停

- 用户连续测试播放/暂停控制。
- 用户确认“播放暂停正常”。
- 当前状态：**Apple Music 播放/暂停控制人工验收通过**。

### 2026-08-26：Apple Music 上一首/下一首首次验收

- 用户反馈“好像不能切歌”，本项暂不通过。
- 系统层复核：直接向 Apple Music 发送 `next track` 后，当前曲目的 persistent ID 已变化，证明系统与 Apple Events 底层切歌能力有效。
- 初步原因：实验 App 在发送指令后立即刷新，Apple Music 尚未完成异步切换，界面仍显示旧曲目，造成未切换的视觉误判。
- 修正：发送控制指令后等待 800ms，再读取播放器状态与曲目信息。
- 复验结果：实验 App 与 Apple Music 均切换到下一首；由于切歌前播放器处于暂停状态，切换后按系统语义继续保持暂停。
- 用户复验：上一首控制无异常。
- 当前状态：**Apple Music 状态读取与全部基础控制人工验收通过**。

### 2026-08-26：电池与音量读取

- 实验 App 显示：电量 26%、正在充电、AC Power、输出音量 50%。
- 系统独立读取：电量 26%、正在充电、AC Power、输出音量 50%。
- 验收结论：电量、充电状态、电源类型和输出音量全部一致。
- 发现问题：充电状态下实验 App 错误显示“剩余时间 0 分钟”，而系统预计约 2 小时 15 分钟充满。
- 修正：充电时改读 `kIOPSTimeToFullChargeKey` 并显示“预计充满”；使用电池时读取 `kIOPSTimeToEmptyKey` 并显示“预计可用”。
- 修正后复验：实验 App 显示电量 30%、预计充满 126 分钟；紧接着系统读取为电量 31%、预计充满 125 分钟，差值来自充电与采样时间推进，数据一致。
- 当前状态：**电池、电源、充电预计时间和输出音量人工验收通过**。

### 2026-08-26：文件拖入与暂存

- 用户从 Finder 将真实文件拖入实验 5 虚线框。
- 实验 App 成功显示对应文件名。
- 当前状态：**Finder 拖入、沙盒授权读取和 App 内暂存人工验收通过**。

### 2026-08-26：文件拖回 Finder

- 用户将实验列表中的暂存文件拖入 Finder“下载”文件夹。
- Finder 成功生成可见文件副本。
- 当前状态：**App 文件拖出与 Finder 接收人工验收通过**。

### 2026-08-26：AirDrop 系统流程

- 用户点击暂存文件右侧的 AirDrop 按钮。
- macOS 成功弹出系统 AirDrop 分享窗口。
- 按第一阶段安全规则未要求实际发送文件。
- 当前状态：**AirDrop 系统分享流程唤起人工验收通过**。
- 用户关闭 AirDrop 窗口后确认暂存文件仍在列表中。
- 当前状态补充：**用户取消分享不会丢失实验副本**。

### 2026-08-26：实验副本清理边界

- 新增“清理实验副本”按钮。
- 自动化验证：同名文件生成不同副本；清理后实验副本消失；Finder 原文件仍存在。
- 用户完成真实文件清理验收并反馈“清理正常”（输入法显示为“清理政策”）。
- 清理边界：只允许删除 App 的 `TestShelf` 目录内、由实验创建的副本，不删除 Finder 原文件。
- 当前状态：**文件清理范围与原文件保护人工验收通过**。
