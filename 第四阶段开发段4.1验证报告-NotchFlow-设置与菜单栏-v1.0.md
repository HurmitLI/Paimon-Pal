# NotchFlow 第四阶段开发段 4.1 验证报告｜设置与菜单栏

> 验证日期：2026-08-27
> 当前分支：`phase/04-settings-permissions-recovery`
> 结论：通过，允许进入开发段 4.2

## 1. 本段交付

- 原生菜单栏状态项和快捷菜单。
- 普通“NotchFlow 设置”窗口。
- 开机启动和菜单栏图标通用设置。
- 暂停 1 小时、暂停至明天、立即恢复。
- 暂停截止时间持久化和过期恢复。
- 暂停期间隐藏刘海，停止 Apple Music 与系统状态轮询；计时器继续按目标结束时间运行。
- 双击或重新打开正在运行的 App 时打开设置窗口。

## 2. 自动化验证

- `xcodebuild test`：47/47 通过，0 失败，0 跳过。
- 新增 5 项设置测试：默认值、一小时暂停、暂停至明天、过期清理、立即恢复。
- 新增 2 项菜单栏测试：运行菜单可触发设置与展开回调；暂停菜单保留恢复入口并禁用展开。
- Debug App 构建和本地签名成功。
- `git diff --check` 通过。

## 3. 真实 Mac 验证

### 产品经理确认

- 菜单栏胶囊图标存在。
- 点击菜单栏图标后快捷菜单正常出现。
- 点击“打开设置…”后“NotchFlow 设置”普通窗口正常出现。

### AI 实际界面操作

- “显示菜单栏图标”关闭后状态变为关闭，重新开启后恢复。
- “暂停 1 小时”后设置页显示暂停及准确恢复时间。
- “暂停至明天”后显示次日 00:00 自动恢复。
- “立即恢复”后回到正常运行状态。
- 暂停状态下重启 App，暂停截止时间和运行状态正确恢复。
- 测试结束后已清除暂停状态，NotchFlow 保持正常运行。

### 开机启动真实验证

- 工作区 Debug App 未安装时，系统返回当前构建不可注册，UI 没有冒充成功。
- 将同一 Debug App 临时复制到用户应用目录后，成功开启“登录后自动启动”。
- 随后成功关闭，最终状态为“不随登录自动启动”。
- 临时安装已移至废纸篓 `NotchFlow-4.1-test.app`，可恢复；用户应用目录没有遗留第二份 NotchFlow。

## 4. 安全与边界

- 没有请求新的系统隐私权限。
- 暂停会强制保留菜单栏图标，避免用户失去恢复入口。
- 隐藏菜单栏图标后仍可从刘海右键菜单打开设置。
- 未点击“退出 NotchFlow”。
- 未改变用户原有文件、计时器或文件架副本。

## 5. 出口结论

设置窗口、菜单栏入口、菜单栏图标开关、暂停与恢复、暂停跨重启恢复、开机启动开启与关闭均得到真实验证。开发段 4.1 通过，下一步进入开发段 4.2“功能开关与渐进权限”。
